# V5-ENGINE Wave-A audit — stage-linear S1–S11 to node-depth discovery

## Audit identity and bound

| Claim ID | Source fact | Proposed design / consequence | Evidence tag |
|---|---|---|---|
| B01 | The approved lane is a bounded, local, read-only workflow audit; its exclusive output is `research/lanes/v5_engine_audit.md`, with at most 30 files and 60 minutes (`reports/sk_v5_wave_a_research_manifest.json:45-71`). The approval excludes implementation, fits, score-bearing experiments, dependencies, external data bodies/actions, and 2024 evaluation (`reports/sk_v5_approval_receipt.json:12-31`). | This audit inspected 20 targeted files, used no network, did not execute tests, fits, metrics, or models, and writes only this file. | directly_supported |
| B02 | The approved skeleton bytes match SHA-256 `c2c813475ab5f0a741e6adbceab196d5b38447f2aeb02f9716a8f5be8973c820`; the manifest bytes match `4690826f4f2f629220c085ffc87883c1ec2fa8990f7f92430483ff4f975ae312`; the approval receipt names the same skeleton hash (`reports/sk_v5_approval_receipt.json:3-10`; `reports/sk_v5_wave_a_research_manifest.json:5-12`). | Treat SK@v5, not the older stage-linear interpretation, as the design authority. | directly_supported |
| B03 | Project rules reserve source writes, fits, lockbox actions, and submissions to the root while allowing bounded read-only research lanes to write under `research/` (`AGENTS.md:3-4`). | All implementation and test execution described below is a DS@v5 proposal for the root, not work performed by this lane. | directly_supported |

**Tag convention.** Every material audit claim is assigned exactly one tag in its row. “Source fact” is descriptive evidence; “Proposed design” is normative and is never presented as an observed implementation fact. Untagged headings, field names, and pseudocode are definitions rather than evidence claims.

## Verdict

| Claim ID | Source fact | Proposed design / consequence | Evidence tag |
|---|---|---|---|
| V01 | The current engine orders eleven stages, computes `depth` from the stage ordinal, stores one mutable record per stage, expands only directly supported proposals, and marks a stage complete after retired emissions or an explicit unknown (`src/baram/loop/stages.py:32-34,77-98,107-124`; `src/baram/loop/research.py:18-20,102-130`; `src/baram/loop/graph_staged.py:59-81,251-295,456-514`). It has no `node_depth` field or graph-level result-to-child transition. | **Verdict: NOT TRUE NODE-DEPTH DISCOVERY.** This contradicts the premise that the present S1–S11 loop already descends a result-conditioned discovery tree. Keep the stage taxonomy and governance checks, but add an immutable lineage schema, a provisional migration-hypothesis layer, typed residual transitions, causal stage visits, explicit pause/success states, and one transactional lineage authority. | contradicts_premise |
| V02 | PREDECLARED/CLOSED validation, hash chaining, monotone comparison indexing, atomic SQLite event append, deterministic registry serialization, chronology/evidence checks, and target-not-reached discrimination already exist (`src/baram/loop/events.py:79-203,239-346,391-453`; `src/baram/loop/registry.py:55-78,121-136`; `src/baram/loop/evaluation.py:31-88`). | Repair rather than replace these contracts; v5 should extend them with body-versioned lineage and make EventStore the authoritative transaction domain. | directly_supported |

## Current invariant matrix

| ID | Current source fact | Exact local evidence | Consequence for v5 | Evidence tag |
|---|---|---|---|---|
| I01 | The stage taxonomy contains S1–S11 in the requested semantic order. | `src/baram/loop/stages.py:37-71`; asserted by `tests/loop/test_stages.py:3-13`. | Preserve stage IDs and derive `stage_depth` from their fixed ordinal. | directly_supported |
| I02 | Every stage except S1 depends only on the immediately preceding stage, and `first_incomplete` returns the earliest dependency-eligible unfinished stage. | `src/baram/loop/stages.py:26-34,77-84`. | This is a stage-linear scheduler, not a parent/child node traversal. | directly_supported |
| I03 | The runtime field named `depth` is exactly the current stage position plus one. | `src/baram/loop/graph_staged.py:59-81`. | Rename/alias it to `stage_depth`; introduce an independently derived `node_depth`. | directly_supported |
| I04 | A stage has a fixed REVISE → RESEARCH → EXPAND → EXECUTE → GATE phase sequence selected from one stage record. | `src/baram/loop/stages.py:87-98`; `src/baram/loop/graph_staged.py:127-149`. | Node type and lineage status must drive the v5 transition, while stage order remains a constraint. | directly_supported |
| I05 | Research request and findings paths are keyed only by `stage_id`; another visit to the same stage addresses the same files. | `src/baram/loop/research.py:11,43-48`; `src/baram/loop/graph_staged.py:151-176,197-215`. | Re-entry/deepening can overwrite or ambiguously reuse stage research; v5 paths need `stage_visit_id/node_id`. | directly_supported |
| I06 | `Finding` stores claim/source/tag/relevance/proposals, but no source-scope gaps, local mapping, falsifier, material degrees of freedom, or admissibility status. | `src/baram/loop/research.py:22-40`. | Add a separate typed migration-hypothesis record; do not overload a source finding. | directly_supported |
| I07 | Only `directly_supported` findings emit nodes; near matches and insufficient findings emit none, while an all-near/insufficient set becomes `explicit_unknown`. | `src/baram/loop/research.py:12-20,98-130`; asserted by `tests/loop/test_research_contract.py:16-25`. | Retain the evidence meanings, but allow a near match to navigate to a **provisional, non-executable** hypothesis. | directly_supported |
| I08 | An empty emission is considered complete when `explicit_unknown=True`; all retired non-bookkeeping emissions are otherwise enough for stage completion. | `src/baram/loop/stages.py:107-124`; asserted by `tests/loop/test_stages.py:16-18`. | An explicit unknown must create a residual deficit plus an advance/re-entry/pause audit, never silently prove discovery completeness. | directly_supported |
| I09 | `NodeSpec` has execution metadata and mutable lifecycle fields but no parent IDs, stage ID, stage/node depth, node type, local deficit, migration contract, falsifier, or child rule. Stage is stored ad hoc in `arguments`. | `src/baram/loop/registry.py:17-39`; construction at `src/baram/loop/graph_staged.py:251-265`. | A versioned immutable node contract is required; lifecycle must be a projection of committed transitions. | directly_supported |
| I10 | PREDECLARED already carries `parent_ids`, mechanism, treatment, control, metric, minimum effect, and basis evidence IDs, but those fields are not cross-checked against a registry lineage contract. | `src/baram/loop/events.py:109-136`; dispatch checks only cycle/node/hash and score class at `src/baram/loop/graph_staged.py:317-324`. | This is useful governance plumbing but only a near match to atomic parent/child lineage. | near_match_only |
| I11 | CLOSED already carries a free-form `next_handoff` list; the snapshot inserts those strings into `frontier`, but EventStore does not require them to be declared registry children. | `src/baram/loop/events.py:138-182,401-435`. | Replace untyped handoffs with validated child declarations/IDs committed with the parent transition. | directly_supported |
| I12 | Evaluation retires the current spec and updates only result/best/stagnation projections; it does not register an adjudication or residual child. The next gate marks the stage done. | `src/baram/loop/graph_staged.py:337-454,456-514`. | Every valid result needs a typed adjudication followed by an atomic result-conditioned residual transition unless success is proven. | directly_supported |
| I13 | `reopen_from` destructively resets the selected stage and every downstream stage; the live graph’s contradiction path merely marks the current record for revision. No result-status path invokes upstream re-entry. | `src/baram/loop/stages.py:127-150`; `src/baram/loop/research.py:98-99`; `src/baram/loop/graph_staged.py:244-249`. | Preserve historical stage visits append-only and create a new causally linked upstream visit instead of erasing prior snapshots. | directly_supported |
| I14 | Router eligibility is ontology-precondition based; ordering uses score-bearing class, category load, saturation, and ID, not parent completion, active lineage, or node depth. | `src/baram/loop/router.py:14-62,88-116`. | Add lineage eligibility and a deterministic depth-first active-frontier order. | directly_supported |
| I15 | `NodeSpec.preconditions` and `trial_budget` exist, but routing does not read either field. | `src/baram/loop/registry.py:27-31`; `src/baram/loop/router.py:14-62`. | Enforce node prerequisites and attempts from the committed contract, not ontology maturity alone. | directly_supported |
| I16 | An unknown ontology subcapability returns no unmet preconditions and is therefore eligible; non-EMPTY maturity satisfies a capability parent. | `src/baram/loop/ontology.py:50-68`; `src/baram/loop/router.py:22-28`. | Unknown capability IDs must fail closed, and capability maturity must remain separate from discovery lineage. | directly_supported |
| I17 | The structure-only ontology is schema v1 and records category, preconditions, maturity, evidence nodes, and research status, without an explicit S1–S11 `stage_id`; preprocessing and feature records illustrate category-string coupling. | `artifacts/registry/capability_ontology_v1.json:1-23,230-335,337-466`; loader/index at `src/baram/loop/ontology.py:9-21`. | Add an explicit stage mapping in a versioned structure-only ontology; do not infer stage from Korean category text. | directly_supported |
| I18 | One target-reaching score causes delivery; all stages done below target produces `target_not_reached`. | `src/baram/loop/evaluation.py:82-88`; `src/baram/loop/graph_staged.py:83-97`; codified at `tests/loop/test_evaluation_contract.py:67-70` and `tests/loop/test_graph_contract.py:95-117`. | SK@v5 requires two deterministic qualifying reproductions, so the single-score delivery test and API must change. | directly_supported |
| I19 | A cycle limit or completed below-target stage list routes to generic halt; complete stage coverage plus no pending/frontier returns `HALT_ALLOWED_EXHAUSTED`. | `src/baram/loop/graph_staged.py:98-125`; `src/baram/loop/events.py:443-453`; codified at `tests/loop/test_events.py:182-203`. | Map bounded exhaustion/budget/access blocks to resumable `RESEARCH_PAUSE`, never success. | directly_supported |
| I20 | Event append is atomic inside the event SQLite transaction, while registry persistence is a separate atomic JSON replacement. Closure is appended before registry retirement and registry save occurs later. | `src/baram/loop/events.py:220-346`; `src/baram/loop/registry.py:121-136`; `src/baram/loop/graph_staged.py:371-388,403-425,516-522`; the ordering is explicitly tested at `tests/loop/test_graph_contract.py:181-250`. | A crash can leave CLOSED in EventStore while the registry still exposes a candidate; two individually atomic stores do not provide one atomic lineage transition. | directly_supported |
| I21 | Expansion registers a list one item at a time; a later conflict returns halt, whose handler saves the already-mutated registry. | `src/baram/loop/graph_staged.py:251-270,520-528`; `src/baram/loop/registry.py:55-63`. | Node declaration must validate and commit as one batch. | directly_supported |
| I22 | Registry invalidation reuses a closed node ID by resetting it to candidate, but EventStore permits only one event of each type per cycle and rejects a different retry. | `src/baram/loop/registry.py:103-119`; `src/baram/loop/events.py:224-235,239-253`. | Never rerun a closed node ID; a revision/reproduction is a new child node/attempt linked to the immutable old node. | directly_supported |
| I23 | Event-chain verification reconstructs and re-canonicalizes the original envelope JSON. | `src/baram/loop/events.py:348-399`; tamper/restart behavior at `tests/loop/test_events.py:111-125`. | Do not add a default top-level `CycleEnvelope` field that would reserialize old rows differently; version new contracts inside event bodies or new event types. | directly_supported |

## Defect-to-minimal-delta map

| Defect | Minimal code/state delta | Exact change surface | Source fact separated from proposed design | Evidence tag |
|---|---|---|---|---|
| D01 — conflated depths | Add `Stage.stage_depth`; keep legacy `depth` as a read alias. Add immutable `stage_id`, `stage_depth`, and derived `node_depth` to every v5 node and active state. | `stages.py`: `_stage`, validation helpers. `graph_staged.py`: `observe`, state normalizer. `registry.py`: `NodeSpecV3`. | Source fact: current `depth` is stage ordinal (`src/baram/loop/graph_staged.py:74-78`). Proposed design: `node_depth = 0` for a root and `1 + max(parent.node_depth)` otherwise; re-entry can lower stage depth while increasing node depth. | directly_supported |
| D02 — no lineage contract | Version `NodeSpec` to v3 and make immutable contract fields first-class; mutable status/outcome/event hashes move to a lifecycle projection. `id` remains an alias for `node_id`. | `registry.py`: dataclasses, validators, v2 loader adapter, query projections. | Source fact: current fields are listed at `src/baram/loop/registry.py:17-36`. Proposed design: use the schema in the next section and reject missing parents, cycles, forged depths, or parent-edge mismatch. | directly_supported |
| D03 — finding directly becomes executable node | Keep `Finding` as source evidence. Add `MigrationHypothesis` plus `derive_provisional_hypotheses` and `admit_experiment`. A near match may create only `PROVISIONAL`; experiment admission requires a directly supported source mechanism or directly supported local diagnostic and a complete migration contract. | `research.py`: dataclasses/validation/expansion split; `graph_staged.py`: separate hypothesis/admission nodes. | Source fact: `expand` currently admits only direct findings and emits executable-shaped dicts (`src/baram/loop/research.py:112-130`). Proposed design preserves tag semantics while adding a non-executable inference layer. | directly_supported |
| D04 — stage-scoped research collision | Write new requests/findings to `research/nodes/{stage_visit_id}/{node_id}/`; include parent result hash, active deficit, query bound, and prior rejected hypotheses. Read legacy `research/stages/S*_findings.json` only as a compatibility fallback. | `research.py`: `request_path`, `findings_path`, `emit_request`, `load_findings`; `graph_staged.py`: callers. | Source fact: both paths currently use only stage ID (`src/baram/loop/research.py:43-48`). Proposed design makes repeated same-stage descent auditable and idempotent. | directly_supported |
| D05 — no result-conditioned children | Replace evaluate→ledger with evaluate→atomic adjudicate-and-spawn. Every SUPPORTED/REFUTED/INCONCLUSIVE result creates an adjudication node and at least one residual child, except a validated success transition; a pause leaves the residual open. Validate spawned children against the parent’s frozen `child_generation_rule`. | `graph_staged.py`: `evaluate`, new `adjudicate`, `transition`; `events.py`: atomic transition method; `registry.py`: projection. | Source fact: evaluate currently ends at retire/best/stagnation (`src/baram/loop/graph_staged.py:415-454`). Proposed design prevents accepted/rejected from becoming a terminal scientific conclusion. | directly_supported |
| D06 — contradiction-only/destructive re-entry | Replace destructive `reopen_from` in v5 flow with `open_stage_visit(target_stage, causal_result_event, reason, affected_snapshots)`. Prior visits remain immutable; downstream snapshots become `STALE`, not deleted. Contradiction uses the same API as a mandatory reason. | `stages.py`: `StageVisit`, re-entry validation, legacy wrapper. `graph_staged.py`: transition branch. | Source fact: current helper resets records (`src/baram/loop/stages.py:127-150`) and live revision is contradiction-triggered (`src/baram/loop/research.py:98-99`). Proposed design permits leakage, data quality, feature insufficiency, regime collapse, calibration failure, and validation instability to re-enter causally. | directly_supported |
| D07 — router is not depth-first | Eligibility requires a declared node, satisfied typed parent edges, compatible stage visit, remaining trial budget, and any ontology prerequisites. Ordering is: mandatory causal/re-entry child, descendant of `active_path`, greatest `node_depth`, declared fidelity/priority, stable ID. Unknown capability IDs fail closed. | `router.py`: `_unmet`, `eligible_specs`, `_ordered_specs`, `route`; `ontology.py`: strict lookup. | Source fact: current order ignores lineage/depth and unknown capabilities pass (`src/baram/loop/router.py:31-62`; `src/baram/loop/ontology.py:56-68`). Proposed design supplies deterministic true-depth traversal without confusing capability prerequisites with genealogy. | directly_supported |
| D08 — individually atomic, jointly inconsistent stores | Put authoritative node contracts, parent edges, lifecycle status, and event rows in the EventStore SQLite transaction domain. Make `SpecRegistry` a query/runner-binding facade and JSON a derived export. Add transactional `declare_batch`, `predeclare_node`, and `close_and_spawn`. | `events.py`: schema migration and transaction APIs. `registry.py`: facade/export. `graph_staged.py`: require registry/store identity and remove post-event `retire/save` writes. | Source fact: event append and JSON save are separate (`src/baram/loop/events.py:239-346`; `src/baram/loop/registry.py:121-136`). Proposed design eliminates the crash window rather than attempting an unsafe two-file commit. | directly_supported |
| D09 — closed ID can be reopened | Deprecate mutable `invalidate` for v5 nodes. Append an invalidation/revival transition and declare a new child with `SUPERSEDES` or `REVISES` edge and a new deterministic node ID. | `registry.py`: compatibility wrapper requiring replacement spec. `events.py`: transition validation. | Source fact: current invalidation resets the same ID despite EventStore’s unique close (`src/baram/loop/registry.py:103-119`; `src/baram/loop/events.py:224-253`). Proposed design keeps all old closures immutable. | directly_supported |
| D10 — single hit is success | Replace `termination_action(best_score, target, all_stages_done)` with a typed `success_eligibility(event_projection, target)`; require two distinct qualifying closures sharing one pipeline-lineage/reproduction key and complete policy/alignment/chronology lineage. Keep best score as telemetry only. | `evaluation.py`: success contract. `events.py`: reproduction projection and `SUCCESS` transition. `graph_staged.py`: router. | Source fact: current delivery needs only one numeric comparison (`src/baram/loop/evaluation.py:82-88`). Proposed design implements SK@v5’s two-reproduction exit (`.planning/2026-08-01-leaderboard-top-4-loop/SK_v5.md:200-205`). | directly_supported |
| D11 — generic halt conflates outcomes | Replace `halt` routing with `pause`, `fail_closed`, and `success` nodes. Persist `pause_reason`, resume requirements, open frontier, and authority/budget evidence. Preserve a legacy `halted` projection only for old callers. | `router.py`: typed disposition. `graph_staged.py`: terminal handlers/state. `events.py`: PAUSED/SUCCESS transition projection. | Source fact: current code routes cycle budget and exhaustion to halt (`src/baram/loop/graph_staged.py:98-125`). Proposed design makes bounded exhaustion resumable and non-success. | directly_supported |
| D12 — binary stage completion | Replace `done` as authority with versioned `StageSnapshot(status=PROVISIONALLY_COMPLETE|ACTIVE|STALE)`. Gate requires deficit artifact, material coverage, migration matrix, feasible experiment adjudication, residual child, and appropriateness disposition. Legacy `done` is a projection. | `stages.py`: completeness validator. `graph_staged.py`: `stage_gate`. `ontology.py`: material coverage. | Source fact: current gate accepts retirement/explicit unknown plus any stage ledger coverage (`src/baram/loop/stages.py:107-124`; `src/baram/loop/graph_staged.py:456-514`). Proposed design implements SK@v5 stage completeness (`.planning/2026-08-01-leaderboard-top-4-loop/SK_v5.md:135-146`). | directly_supported |
| D13 — ontology category is used as structure | Add structure-only ontology schema v2 fields `stage_id`, `materiality`, and `coverage_contract_version`; validate IDs and stage mapping. Do not mutate evidence outcomes in the ontology. | `ontology.py` and a separately versioned capability ontology artifact. | Source fact: v1 has category/maturity/preconditions but no explicit stage mapping (`artifacts/registry/capability_ontology_v1.json:1-23,230-466`). Proposed design makes the ontology a capability map, while EventStore remains the evidence/lifecycle authority. | directly_supported |
| D14 — typed state definition is outside this lane’s readable set | DS@v5 must authorize and inspect the imported `ResearchState` definition before implementation, then add the v5 fields/reducers or deliberately define a v5 state type in an authorized module. | `src/baram/loop/graph_staged.py:23` imports `baram.loop.state.ResearchState`; `state.py` was not in manifest’s allowed source set (`reports/sk_v5_wave_a_research_manifest.json:57-70`). | Source fact: the runtime type is external to the audited files. Proposed design: use the exact state schema below, but do not claim its current reducer compatibility until the root audits `state.py`. | insufficient |

## Proposed node and state schema

### Node contract v3

| Schema part | Exact proposed fields and invariant | Source fact | Evidence tag |
|---|---|---|---|
| Identity | `schema_version=3`, `node_id`, `contract_sha256`, `node_type`, `stage_visit_id`, `stage_id`, `stage_depth`, `node_depth`, `subcapability`, `origin`. `stage_depth` is derived from S1–S11; `node_id` is a deterministic hash of the canonical immutable contract excluding `node_id/contract_sha256`. | SK@v5 requires node/stage identity and separate depths (`.planning/2026-08-01-leaderboard-top-4-loop/SK_v5.md:100,121-133`); current registry lacks them (`src/baram/loop/registry.py:17-36`). | directly_supported |
| Parent graph | `parent_ids: tuple[str,...]` plus `parent_edges: tuple[{parent_id, relation, causal_event_sha256?},...]`; set equality is mandatory. Relations: `DERIVED_FROM`, `RESULT_RESIDUAL`, `ADVANCES_STAGE`, `REENTERS_UPSTREAM`, `REVISES`, `REPRODUCES`. Roots alone may have no parent. Non-root depth is `1 + max(parent.node_depth)`; cycles and missing parents fail closed. | Event PREDECLARED has unvalidated parent IDs (`src/baram/loop/events.py:110-136`), while registry has none. | directly_supported |
| Node types | `FOUNDATION_SNAPSHOT`, `STAGE_DEFICIT`, `RESEARCH`, `MIGRATION_HYPOTHESIS`, `CONTROLLED_EXPERIMENT`, `ADJUDICATION`, `RESIDUAL_DEFICIT`, `APPROPRIATENESS_AUDIT`. Re-entry is an edge/disposition, not a misleading new pipeline stage. | SK@v5’s canonical loop names deficit, research, migration hypotheses, experiment, adjudication, and residual child (`.planning/2026-08-01-leaderboard-top-4-loop/SK_v5.md:73-100`). | directly_supported |
| Deficit/evidence | `local_deficit_id`, `diagnostic_artifact={path,sha256}`, `research_claim_ids`, `basis_local_evidence_ids`. Source findings retain exactly one evidence tag; local experiment verdict is a separate tri-state field. | SK@v5 requires exact diagnostic and separate source/local evidence (`.planning/2026-08-01-leaderboard-top-4-loop/SK_v5.md:102-119,125-133`). | directly_supported |
| Migration hypothesis | `migration_contract`, `hypothesis`, `mechanism`, `falsifier`, and `hypothesis_status=PROVISIONAL|ADMISSIBLE|REJECTED`. `PROVISIONAL` is never dispatchable. | Current finding/node schemas cannot represent this layer (`src/baram/loop/research.py:22-40`; `src/baram/loop/registry.py:17-36`). | directly_supported |
| Experiment contract | `treatment`, `control`, `degrees_of_freedom`, `candidate_family`, `trial_budget`, `estimated_compute`, `preconditions`, `reference`, `arguments`, `score_bearing`, and `governance_checks`. All material choices are frozen before PREDECLARED. | Current PREDECLARED covers only part of this contract (`src/baram/loop/events.py:110-136`); SK@v5 lists the rest (`.planning/2026-08-01-leaderboard-top-4-loop/SK_v5.md:108-115,129-132`). | directly_supported |
| Evaluation/children | `evaluation_contract={metric,min_effect,component_gates,stability_gate,multiplicity_gate,row_alignment_keys}`, `child_generation_rule`, `closure_premise`, `revival_premise`. The child rule maps each tri-state result to allowed child types/stage relations, maximum children, and remaining budget. | Current `trial_budget` is unenforced and result closure emits no child (`src/baram/loop/registry.py:27-33`; `src/baram/loop/graph_staged.py:415-454`). | directly_supported |
| Lifecycle projection | `status=PROVISIONAL|READY|PREDECLARED|RUNNING|CLOSED|SUPERSEDED|BLOCKED`; `verdict=None|SUPPORTED|REFUTED|INCONCLUSIVE|NOT_APPLICABLE`; `disposition=None|PROMOTE|DEEPEN|ADVANCE|BRANCH|REENTER|FAIL_CLOSED_NODE|RESEARCH_PAUSE|SUCCESS_EXIT`; plus authoritative event hashes. These are projected from EventStore rather than edited in JSON. | Current status/outcome/history are mutable registry fields (`src/baram/loop/registry.py:31-36,89-119`). | directly_supported |

### Migration contract v1

| Field group | Exact proposed content | Source fact | Evidence tag |
|---|---|---|---|
| Evidence basis | `source_claim_ids`, `local_diagnostic_ids`, each resolved to an immutable locator/hash and evidence class. | Current proposal retains only one origin string (`src/baram/loop/research.py:119-128`). | directly_supported |
| Scope-gap matrix | Required keys: `population`, `geography`, `horizon`, `issue_time`, `inputs`, `target`, `metric`, `resolution`, `topology`, `compute`, `licence`; each value records source scope, local scope, and unresolved gap. | SK@v5 enumerates these admission gaps (`.planning/2026-08-01-leaderboard-top-4-loop/SK_v5.md:108-114`). | directly_supported |
| Local mapping | Exact transform/model/policy, input availability and deployment symmetry, chronology proof, no-2024 proof, licence receipt where applicable, parent/control, and row keys. | SK@v5 requires exact local mapping and governance proof (`.planning/2026-08-01-leaderboard-top-4-loop/SK_v5.md:110-117`). | directly_supported |
| Falsification/budget | Expected effect channel, falsifier, material DOF, bounded family, trial/compute budget, and predeclared result-conditioned child rule. | Current `Finding.validate` checks only proposal subcapability/summary (`src/baram/loop/research.py:31-40`). | directly_supported |
| Admission | `PROVISIONAL` may cite a near match for navigation; `ADMISSIBLE` requires at least one directly supported source mechanism **or** directly supported local diagnostic plus every required field above. No evidence tag is upgraded during admission. | SK@v5 explicitly permits provisional navigation but not transfer claims from near evidence (`.planning/2026-08-01-leaderboard-top-4-loop/SK_v5.md:102-119`). | directly_supported |

### ResearchState v5 projection

| State group | Exact proposed fields / invariant | Source fact | Evidence tag |
|---|---|---|---|
| Version/control | `state_schema_version=5`, `cycle`, `workflow_status=RUNNING|RESEARCH_PAUSE|SUCCESS_EXIT|ENGINE_FAIL_CLOSED`, `route`, `phase`. | Current state updates carry cycle/route/phase but generic halt (`src/baram/loop/graph_staged.py:71-80,135-149,520-535`). | directly_supported |
| Active position | `stage_visit_id`, `stage_id`, `stage_depth`, `active_node_id`, `node_depth`, `active_path_ids`, `frontier_node_ids`, `active_deficit_id`. Every value is reconciled from the EventStore projection on observe/restart. | Current observe derives only stage, ordinal depth, and candidate IDs (`src/baram/loop/graph_staged.py:59-81`). | directly_supported |
| Historical snapshots | `stage_snapshots: {stage_id: [StageSnapshot...]}` where each snapshot has visit ID, status, deficit/research/migration/adjudication/residual/audit node IDs, closure hash, and invalidation cause. No visit is overwritten. | Current `stage_history` stores one mutable record per stage and `reopen_from` resets it (`src/baram/loop/stages.py:127-150`). | directly_supported |
| Pending transition | `pending_transition={parent_node_id,result_event_sha256,result_status,allowed_dispositions,selected_disposition,child_ids}` and optional `pending_reentry={target_stage_id,reason,affected_snapshot_ids}`. | Current result is reduced directly to best/stagnation with no child transition (`src/baram/loop/graph_staged.py:426-454`). | directly_supported |
| Pause/success | `pause_contract={reason_code,resume_requirements,authority_or_budget_evidence,resumable:true}` or `success_contract={reproduction_event_ids,pipeline_lineage_digest,reproduction_key}`; the two are mutually exclusive. | Current generic stop reason cannot prove pause versus success (`src/baram/loop/graph_staged.py:93-125,520-535`). | directly_supported |
| Compatibility telemetry | Retain `best_score`, `best_spec`, `stagnation`, `results`, `ledger`, `event_snapshot`, `stage_history`, `stage`, and `depth` as non-authoritative projections during migration. `depth` mirrors `stage_depth`, never `node_depth`. | These keys are read or written throughout `src/baram/loop/graph_staged.py:59-149,297-535`. | directly_supported |
| Reducers | List/dict reducer behavior and old-checkpoint deserialization must be verified before coding because the defining `ResearchState` file was outside this audit. | The only audited fact is the external import at `src/baram/loop/graph_staged.py:23`; no allowed file defines the reducers. | insufficient |

## Proposed transition/state machine

| From | Guard | Atomic action | Next | Source fact separated from proposed design | Evidence tag |
|---|---|---|---|---|---|
| START / resumed pause | Event chain and node projection verify; resume requirements satisfied. | Reconcile checkpoint from EventStore; select existing active frontier, never create duplicate nodes on retry. | active node or `STAGE_DEFICIT` root | Source fact: current snapshot verifies the chain (`src/baram/loop/events.py:401-441`). Proposed design: EventStore wins over checkpoint/JSON. | directly_supported |
| `STAGE_DEFICIT` | Exact diagnostic artifact and local deficit ID are frozen. | Declare a node-scoped bounded `RESEARCH` child. | `RESEARCH` | Source fact: current request carries stage/prior summaries only (`src/baram/loop/research.py:57-81`). Proposed design adds deficit/parent context. | directly_supported |
| `RESEARCH` | Findings validate. | If contradiction: declare a revision/re-entry deficit. Otherwise declare zero or more `PROVISIONAL` migration hypotheses; if none, declare a residual unknown and an audit, not stage success. | hypothesis, residual, or audit | Source fact: current contradiction/unknown branches emit no nodes (`src/baram/loop/research.py:98-130`). Proposed design gives both branches explicit lineage. | directly_supported |
| `MIGRATION_HYPOTHESIS:PROVISIONAL` | Direct source mechanism or direct local diagnostic exists; all gaps/mapping/DOF/falsifier/governance fields freeze. | Mark hypothesis admissible and declare bounded `CONTROLLED_EXPERIMENT` child with PREDECLARED contract. | experiment | Source fact: current direct node expansion lacks an admission contract (`src/baram/loop/research.py:112-130`). Proposed design prevents near-match transfer from becoming execution evidence. | directly_supported |
| `CONTROLLED_EXPERIMENT` | Parents/preconditions/budget/predeclaration and runner are valid. | Dispatch once; append closure and an `ADJUDICATION` node in the same transaction. Bookkeeping misses leave it open or pause. | adjudication or pause | Source fact: current dispatch already fails closed for missing predeclaration/runner (`src/baram/loop/graph_staged.py:297-335`). Proposed design retains that guard and makes the transition atomic. | directly_supported |
| `ADJUDICATION` with SUPPORTED | Evidence contract passes. | If first target-reaching reproduction, create a reproduction/residual child; otherwise apply frozen child rule. Only two matching qualifying reproductions may emit SUCCESS. | residual/reproduction, audit, or success | Source fact: current accepted result only updates best/stagnation (`src/baram/loop/graph_staged.py:426-454`). Proposed design implements the approved repeated-success gate. | directly_supported |
| `ADJUDICATION` with REFUTED | Valid closure establishes the bounded treatment failed its gate. | Close this node/family, then atomically create a residual-mechanism or bounded sibling child allowed by the frozen rule. | residual/sibling | Source fact: current rejection increments stagnation only (`src/baram/loop/graph_staged.py:452-454`). Proposed design makes failure scientifically productive without terminating the workflow. | directly_supported |
| `ADJUDICATION` with INCONCLUSIVE | Valid closure cannot distinguish the hypothesis under its frozen gate. | Create an uncertainty/resolution residual and an allowed diagnostic/research child, or pause when its bound is exhausted. | residual/research or pause | Source fact: CLOSED supports tri-state status (`src/baram/loop/events.py:173-176`) but graph adjudication reduces to accepted boolean (`src/baram/loop/evaluation.py:68-79`). Proposed design retains the tri-state downstream. | directly_supported |
| `RESIDUAL_DEFICIT` → audit | Causal result hash, remaining deficit, and feasible choices are recorded. | Declare `APPROPRIATENESS_AUDIT`; choose exactly one of DEEPEN, BRANCH, ADVANCE, REENTER, FAIL_CLOSED_NODE, or RESEARCH_PAUSE. | chosen disposition | Source fact: current stage gate has verdict summaries but no residual/audit (`src/baram/loop/graph_staged.py:478-514`). Proposed design makes the next-node reason first-class. | directly_supported |
| audit → DEEPEN/BRANCH | Frozen rule permits same-stage child and budget remains. | Declare child with same `stage_id`, greater `node_depth`, and typed parent edge. | deeper/sibling node | Source fact: no current node depth exists (`I03`, `I09`). Proposed design is true within-stage discovery. | directly_supported |
| audit → ADVANCE | Provisional stage snapshot passes all completeness fields. | Close snapshot and declare next-stage deficit with `ADVANCES_STAGE` edge. | next stage visit | Source fact: current advance is implied by `done=True` (`src/baram/loop/stages.py:77-84`). Proposed design requires an explicit audit. | directly_supported |
| audit/result → REENTER | A result records `PREMISE_CONTRADICTION`, `LEAKAGE`, `DATA_QUALITY`, `FEATURE_INSUFFICIENCY`, `REGIME_COLLAPSE`, `CALIBRATION_FAILURE`, or `VALIDATION_INSTABILITY`; target stage is causally upstream. | Mark affected snapshots stale and declare a new upstream deficit visit. Target `stage_depth` decreases; `node_depth` increases. | upstream stage visit | Source fact: SK@v5 authorizes result-driven re-entry (`.planning/2026-08-01-leaderboard-top-4-loop/SK_v5.md:135-146`); current helper is contradiction/destructive only (`src/baram/loop/stages.py:127-150`). | directly_supported |
| any non-success state → RESEARCH_PAUSE | Approval/access/licence/compute/cycle/evidence bound prevents the next authorized action. | Commit pause reason, resume requirements, and open frontier; do not mark the goal complete. | END, resumable | Source fact: approved pause reasons are explicit (`.planning/2026-08-01-leaderboard-top-4-loop/SK_v5.md:200-205`). Proposed design replaces generic halt. | directly_supported |
| eligible reproduction state → SUCCESS_EXIT | Two distinct closures have the same complete pipeline/reproduction key, satisfy strict local official target and governance, and reproduce deterministically. | Commit SUCCESS referencing both closure hashes and lineage digest; freeze registry projection. | END, success | Source fact: this is the sole approved exit (`.planning/2026-08-01-leaderboard-top-4-loop/SK_v5.md:25-33,200-205`). Proposed design makes it event-verifiable. | directly_supported |

### Depth-first routing invariant

| Claim ID | Source fact | Proposed design | Evidence tag |
|---|---|---|---|
| T01 | Current selection has no parent graph or node-depth key (`src/baram/loop/router.py:31-62`). | Among eligible nodes, first satisfy a mandatory causal/re-entry child, then remain on `active_path`, then choose greatest `node_depth`, then the frozen fidelity/priority, then stable `node_id`. Parent completion, trial budget, and stage-visit validity are hard gates rather than sort preferences. | directly_supported |
| T02 | Ontology prerequisites are capability dependencies, not experiment genealogy (`src/baram/loop/ontology.py:50-68`). | Keep `parent_edges` and ontology `preconditions` separate and require both; an accepted capability result cannot fabricate a discovery parent. | directly_supported |

## Atomic EventStore/registry lineage design

| Claim ID | Source fact | Proposed design | Evidence tag |
|---|---|---|---|
| A01 | The current event DB and registry JSON cannot be committed in one transaction (`src/baram/loop/events.py:239-346`; `src/baram/loop/registry.py:121-136`). | Use the existing EventStore SQLite database as the one authoritative write domain. Add `node_specs(node_id, schema_version, contract_json, contract_sha256, status, predeclared_event_sha256, closure_event_sha256, outcome_json, lineage_complete)` and `node_parents(node_id,parent_id,relation,causal_event_sha256)` with foreign keys and deferred validation. | directly_supported |
| A02 | EventStore already serializes writers with `BEGIN IMMEDIATE`, computes the chain hash, validates closure/predeclaration, and enforces comparison order (`src/baram/loop/events.py:239-345`). | Factor append internals into one transaction object and expose `declare_batch`, `predeclare_node`, and `close_and_spawn`; each validates all event/node rows first, writes all rows, then commits once. A failure or injected crash yields neither parent close nor children. | directly_supported |
| A03 | `SpecRegistry` currently persists contracts and lifecycle independently (`src/baram/loop/registry.py:42-53,89-136`). | Make it a facade over the EventStore node tables plus an in-memory runner map. `save()` becomes a deterministic derived JSON export only; graph correctness never depends on export timing. | directly_supported |
| A04 | A CLOSED envelope is matched to PREDECLARED by cycle/hash and score class, but append does not itself compare both envelopes’ node/stage IDs (`src/baram/loop/events.py:270-300`). | Transaction validation must require identical `node_id`, `stage_id`, node contract hash, score class, and attempt identity across declaration, predeclaration, and closure. | directly_supported |
| A05 | `next_handoff` may contain arbitrary strings and snapshot treats them as frontier (`src/baram/loop/events.py:182,401-435`). | In v5 closure, `spawned_child_ids` must exactly equal the child rows inserted in the same transaction; each child must point back to the parent and satisfy the frozen child rule. Preserve `next_handoff` only for v1 event replay. | directly_supported |
| A06 | The graph may be restarted with a durable checkpointer while registry/event snapshots can differ (`src/baram/loop/graph_staged.py:25,557-564`). | At every `observe`, verify event chain, rebuild registry/frontier projection, compare checkpoint digest, and overwrite only the in-memory/checkpoint projection. Never “repair” authoritative events from a checkpoint. | directly_supported |

### Event contract v2 (new rows only)

| Event type | Exact proposed body additions / transaction rule | Source fact | Evidence tag |
|---|---|---|---|
| `NODE_DECLARED` | Full canonical `node_contract`, `node_contract_sha256`, `lineage_complete`; insert the event, `node_specs`, and `node_parents` rows together. `cycle_id=node_id` for a one-shot node declaration. | Current EVENT_TYPES has no node-declaration event (`src/baram/loop/events.py:17-20`). | directly_supported |
| `PREDECLARED` v2 | Retain all v1 fields and require `node_contract_sha256`, `attempt_id`, `child_generation_rule_sha256`, and governance-contract version; append event and attach its hash to the node row together. | Current body already freezes mechanism/treatment/control/metric/basis (`src/baram/loop/events.py:110-136`). | directly_supported |
| `STARTED` | Require `predeclared_event_sha256`, `node_contract_sha256`, `attempt_id`, `runner_reference`, and `runner_contract_sha256`. Commit before dispatch; a STARTED-without-CLOSED restart becomes execution-recovery pause rather than blind rerun. | Current projection distinguishes only predeclared versus closed (`src/baram/loop/events.py:401-435`). | directly_supported |
| `CLOSED` v2 | Retain v1 provenance fields and require `node_contract_sha256`, `attempt_id`, tri-state result, `residual_deficit`, `selected_disposition`, `spawned_child_ids`, and (for reproduction candidates) `reproduction_key`/`output_digest`. Insert closure, parent lifecycle update, and all child declarations in one transaction. | Current CLOSED has tri-state and free `next_handoff` but no typed residual/atomic child (`src/baram/loop/events.py:138-182,270-346`). | directly_supported |
| `NODE_INVALIDATED` | Require immutable prior closure hash, causal evidence/result hash, reason, and replacement child ID; never clear the old row. Use a unique transition `cycle_id` so successive evidence events do not collide. | Current registry invalidation clears outcome under the same ID (`src/baram/loop/registry.py:103-119`). | directly_supported |
| `WORKFLOW_PAUSED` | Require reason enum, resume requirements, open-frontier IDs, stage visit, authority/budget evidence, and event-snapshot digest. | Current generic halt is not persisted as a typed event (`src/baram/loop/graph_staged.py:520-528`). | directly_supported |
| `SUCCESS_DECLARED` | Require two distinct qualifying CLOSED hashes, one reproduction key, output identity/tolerance contract, pipeline-lineage digest, and governance proof. | Current success is a state branch from one score (`src/baram/loop/evaluation.py:82-88`). | directly_supported |
| `REGISTRY_MIGRATED` | Require old registry schema/hash, imported/conflicted counts, migration-code hash, and resulting projection digest; it may only append after a fully committed import. | Current registry JSON advertises schema v2 but is outside the event transaction (`src/baram/loop/registry.py:121-136`). | directly_supported |

**Compatibility rule (directly_supported).** For v1 PREDECLARED/CLOSED/LEGACY rows, validation and canonical serialization remain exactly as today; the proposed new requirements apply only when the body declares the v2 contract or when the event type is new (`src/baram/loop/events.py:79-203,348-399`).

## Backward compatibility and migration strategy

| Step | Source fact | Proposed migration | Evidence tag |
|---|---|---|---|
| M01 — preserve old hashes | Verification hashes the exact canonical old envelope (`src/baram/loop/events.py:391-399`). | Leave the `events` table rows and `CycleEnvelope` top-level shape byte-for-byte compatible. Extend `EVENT_TYPES`; put `node_contract_version=2` only in new event bodies. Validator dispatches old bodies to v1 rules and new bodies to v2 rules. | directly_supported |
| M02 — database migration | Current initialization only creates `events` and has no explicit schema-version migration (`src/baram/loop/events.py:220-237`). | Use `PRAGMA user_version` and an idempotent `BEGIN IMMEDIATE` migration that creates node/parent tables without rewriting events. Append a migration receipt event after successful import; never renumber sequences. | directly_supported |
| M03 — registry v2 import | Loader constructs `NodeSpec(**raw)` from JSON and writer emits schema v2 (`src/baram/loop/registry.py:50-53,121-130`). | Import the canonical v2 JSON in one DB transaction, retain every raw field in `legacy_payload`, and record the source-file SHA. Recover stage/parents only when `arguments.stage` and matching PREDECLARED events verify them; otherwise set `lineage_complete=false` and do not invent ancestry/depth. | directly_supported |
| M04 — unknown live legacy shape | This lane was prohibited from reading shared model/outcome registry state, so it did not inspect the live `loop_node_specs.json` or EventStore rows. | Root must run a structure-only dry-run migration report before mutation: counts by schema/mapping outcome, missing/duplicate IDs, unmatched events, cycles, and proposed defaults; abort on conflict. | insufficient |
| M05 — registry API | Current callers use `spec.id`, `arguments['stage']`, `candidate/retired`, and `save()` (`src/baram/loop/graph_staged.py:63-70,251-265,302-306,516-522`). | Supply read aliases: `id→node_id`, synthesized `arguments['stage']→stage_id`, active statuses→`candidate`, terminal statuses→`retired`; keep deterministic export. New graph code uses typed fields only. | directly_supported |
| M06 — immutable reruns | Current `invalidate` mutates the closed object (`src/baram/loop/registry.py:103-119`). | Legacy invalidation remains readable, but v5 mutation requires a replacement spec and emits `REVISES`; old node/event/outcome remain unchanged. Reproduction also uses a distinct child ID. | directly_supported |
| M07 — research artifacts | Current files are stage-scoped (`src/baram/loop/research.py:43-48`). | Reader order is node-scoped v5 path, then legacy stage path. All new writes use node-scoped paths and include the imported legacy file hash when used. | directly_supported |
| M08 — checkpoint/state | Current graph writes legacy `stage`, `depth`, `stage_history`, and `phase` keys (`src/baram/loop/graph_staged.py:71-80,137-149,503-514,520-535`). | A pure `normalize_state_v5` maps these to compatibility projections, derives the authoritative frontier from EventStore, and never guesses missing parentage. Keep aliases for one release. Exact reducers require the root’s separate `state.py` audit. | insufficient |
| M09 — termination strings | Existing tests and callers may expect `halted` and `HALT_ALLOWED_EXHAUSTED` (`tests/loop/test_events.py:182-203`; `tests/loop/test_graph_contract.py:95-117`). | Preserve those strings only through a named v1 compatibility adapter. Native v5 returns typed `RESEARCH_PAUSE`/`SUCCESS_EXIT`; do not silently call exhaustion success. | directly_supported |
| M10 — ontology | Loader is hardwired to v1 path (`src/baram/loop/ontology.py:9-17`). | Keep v1 readable and introduce a fingerprinted v2 structure artifact with explicit stage IDs; normalize both into one in-memory shape. Do not overwrite v1. | directly_supported |

## Executable test matrix for the root implementation

**Test boundary (directly_supported).** The proposed matrix consists of pure contract/state tests over `tmp_path`/memory checkpointers with synthetic envelopes; by design it requires no raw data, labels, model fit, metric notebook, network, lockbox, or Dacon action (`reports/sk_v5_wave_a_research_manifest.json:63-70`).

| Test file and exact proposed test | Arrange / act / assert | Defect covered | Evidence tag |
|---|---|---|---|
| `tests/loop/test_registry.py::test_v3_depth_is_derived_not_caller_controlled` | Declare root then child; assert stage ordinal and `node_depth=max(parent)+1`; forged depth fails. | D01/D02 | directly_supported |
| `tests/loop/test_registry.py::test_missing_parent_cycle_and_parent_edge_mismatch_fail_closed` | Attempt missing parent, self-cycle, two-node cycle, and mismatched `parent_ids/parent_edges`; assert no rows committed. | D02/D08 | directly_supported |
| `tests/loop/test_registry.py::test_revision_and_reproduction_require_new_node_ids` | Close a node, attempt same-ID reset, then declare `REVISES` and `REPRODUCES` children; assert old closure immutable. | D09 | directly_supported |
| `tests/loop/test_registry.py::test_schema_v2_import_preserves_raw_payload_and_marks_unknown_lineage` | Import a minimal v2 JSON fixture with and without verified stage/event mapping; assert lossless payload and honest `lineage_complete`. | D11 | directly_supported |
| `tests/loop/test_research_contract.py::test_near_match_creates_only_provisional_hypothesis` | Feed near-match finding; assert a hypothesis exists, is non-dispatchable, and evidence tag is unchanged. | D03 | directly_supported |
| `tests/loop/test_research_contract.py::test_hypothesis_admission_requires_direct_or_local_basis_and_complete_gap_matrix` | Omit each basis/gap/mapping/DOF/falsifier field in turn; assert admission fails; complete contract passes. | D03 | directly_supported |
| `tests/loop/test_research_contract.py::test_same_stage_reentry_uses_distinct_node_scoped_paths` | Create two S5 visits; assert request/findings paths differ and legacy fallback is read-only/hash-recorded. | D04 | directly_supported |
| `tests/loop/test_stages.py::test_reentry_lowers_stage_depth_while_node_depth_increases` | Close a downstream result then re-enter an earlier stage with causal edge; assert independent depths. | D01/D06 | directly_supported |
| `tests/loop/test_stages.py::test_reentry_preserves_prior_stage_visits_and_marks_snapshots_stale` | Re-enter after completed snapshots; assert no record deletion and exact invalidation cause. | D06/D12 | directly_supported |
| `tests/loop/test_stages.py::test_explicit_unknown_alone_cannot_complete_stage_snapshot` | Supply no emissions plus explicit unknown; assert residual/audit or pause required. | D12 | directly_supported |
| `tests/loop/test_stages.py::test_snapshot_gate_requires_all_six_completeness_components` | Remove deficit, coverage, migration matrix, feasible adjudication, residual, or audit one at a time; assert fail closed. | D12 | directly_supported |
| `tests/loop/test_router.py::test_deepest_eligible_active_path_descendant_is_selected_deterministically` | Supply eligible parent/sibling/deeper descendants in shuffled orders; assert same deepest active-path choice. | D07 | directly_supported |
| `tests/loop/test_router.py::test_unknown_subcapability_fails_closed` | Route a node absent from ontology; assert explicit contract error/research block, never dispatch. | D07/D13 | directly_supported |
| `tests/loop/test_router.py::test_causal_reentry_child_precedes_unrelated_sibling` | Create an eligible re-entry child and unrelated candidates; assert causal child wins. | D06/D07 | directly_supported |
| `tests/loop/test_router.py::test_trial_budget_and_node_preconditions_are_hard_gates` | Exhaust attempts or leave typed prerequisite open; assert node is ineligible. | D07 | directly_supported |
| `tests/loop/test_events.py::test_declare_batch_is_all_or_nothing_on_conflict` | Batch one valid and one conflicting child; assert neither node/event is committed. | D08/I21 | directly_supported |
| `tests/loop/test_events.py::test_predeclare_updates_event_and_registry_projection_atomically` | Inject failure before commit and after staged update; assert neither side or both sides visible. | D08 | directly_supported |
| `tests/loop/test_events.py::test_close_and_spawn_is_atomic_under_each_failpoint` | Inject failpoints after closure validation, event insert, parent projection update, and child insert; reopen DB and assert all-or-nothing. | D05/D08 | directly_supported |
| `tests/loop/test_events.py::test_spawned_children_exactly_match_typed_parent_edges_and_frozen_rule` | Try orphan, undeclared, excess, wrong-stage, and disallowed result children; assert transaction rollback. | D05/D08 | directly_supported |
| `tests/loop/test_events.py::test_closed_identity_matches_predeclaration_and_node_contract` | Vary stage, node, contract hash, attempt, and score class; assert rejection. | D08 | directly_supported |
| `tests/loop/test_events.py::test_v1_event_chain_prefix_survives_v5_open_and_migration` | Open a fixed v1 event fixture, migrate schema, and assert every pre-migration sequence/envelope byte/hash plus the old tail hash is unchanged; appended migration rows may create a new full snapshot digest. | D11 | directly_supported |
| `tests/loop/test_events.py::test_transaction_retry_is_idempotent_and_conflicting_retry_fails` | Retry identical close+spawn, then mutate a child contract; assert same projection first, conflict second. | D08 | directly_supported |
| `tests/loop/test_events.py::test_pause_persists_open_frontier_and_resume_requirements` | Commit each pause reason; restart and assert non-success, resumability, and identical frontier. | D11 | directly_supported |
| `tests/loop/test_events.py::test_success_event_requires_two_distinct_matching_reproduction_closures` | Try one closure, duplicate ID, mismatched lineage key/output digest, and two matching distinct closures; only final case succeeds. | D10/D11 | directly_supported |
| `tests/loop/test_evaluation_contract.py::test_adjudication_preserves_supported_refuted_inconclusive` | Validate each tri-state closure and assert it remains tri-state rather than collapsing to accepted boolean. | D05 | directly_supported |
| `tests/loop/test_evaluation_contract.py::test_best_score_alone_never_authorizes_success` | Set telemetry above target without two qualifying event refs; assert non-success. | D10 | directly_supported |
| `tests/loop/test_graph_contract.py::test_each_result_status_creates_adjudication_and_residual_child` | Parameterize tri-state result; invoke graph; assert parent close, adjudication, and allowed residual child are one committed transition. | D05 | directly_supported |
| `tests/loop/test_graph_contract.py::test_result_can_causally_reenter_any_earlier_stage_without_erasing_history` | Emit each approved re-entry reason; assert new visit/edge, stale impacted snapshots, preserved events. | D06 | directly_supported |
| `tests/loop/test_graph_contract.py::test_one_target_reproduction_does_not_deliver_and_two_matching_do` | Invoke with one then two qualified reproduction closures; assert RUNNING/reproduction child then SUCCESS_EXIT. | D10 | directly_supported |
| `tests/loop/test_graph_contract.py::test_all_stage_snapshots_below_target_is_research_pause_not_success` | Complete provisional snapshots with no qualifying success; assert resumable bounded-exhaustion pause. | D11/D12 | directly_supported |
| `tests/loop/test_graph_contract.py::test_cycle_budget_is_pause_and_failed_node_does_not_end_viable_sibling` | Exhaust cycle budget and separately refute one family with sibling open; assert pause in first, continued routing in second. | D11 | directly_supported |
| `tests/loop/test_graph_contract.py::test_restart_reconciles_stale_checkpoint_and_registry_export_from_eventstore` | Stop after atomic commit but before checkpoint/export refresh; restart and assert no redispatch/duplicate child. | D08/D11 | directly_supported |
| Existing governance contracts in `test_events.py`, `test_evaluation_contract.py`, and `test_graph_contract.py` | Retain exact tests for predeclaration, chain tamper, receipt hash, comparison monotonicity, chronology/alignment, lockbox prohibition, Dacon prohibition, and runner fail-closed behavior; change only the intentionally superseded single-success/exhaustion assertions. | Regression boundary | directly_supported |

**Execution handoff (directly_supported).** The following is the proposed root command after implementation; this lane did not run it under the read-only research approval (`reports/sk_v5_approval_receipt.json:19-29`):

```bash
PYTHONDONTWRITEBYTECODE=1 python -m pytest -p no:cacheprovider tests/loop -q
```

## Unknowns and bounded non-claims

| Unknown ID | Source fact / unknown | Required DS@v5 resolution | Evidence tag |
|---|---|---|---|
| U01 | `src/baram/loop/state.py` was outside the authorized file set, although `src/baram/loop/graph_staged.py:23` imports its `ResearchState`. Reducer annotations and checkpoint deserialization were therefore not inspected. | Root must audit and fingerprint that file before approving the state diff; otherwise reducer compatibility is unknown. | insufficient |
| U02 | `src/baram/loop/graph.py` was outside the authorized source set, although a current test imports and exercises it (`tests/loop/test_graph_contract.py:7-9,65-103`). | Root must decide whether v5 replaces only `build_staged_graph` or must share compatibility adapters with `build_graph`. | insufficient |
| U03 | The live registry JSON and EventStore database were not opened because the lane excluded shared-state/model outcome reads. The number of legacy nodes with missing stage/parent/event matches is unknown. | Run a root-owned structure-only migration dry run before any DB or registry mutation; retain conflicts as unknown rather than synthesizing lineage. | insufficient |
| U04 | SK@v5 says “two deterministic reproductions” but does not define byte-identical prediction/output digest versus an allowed numerical tolerance (`.planning/2026-08-01-leaderboard-top-4-loop/SK_v5.md:25-33,200-205`). | DS@v5 must freeze the reproduction key, output digest/tolerance, distinct-run rule, and required hash fields before implementation. | insufficient |
| U05 | SK@v5 does not define multi-parent `node_depth`. | DS@v5 should approve or replace the proposed deterministic rule `1 + max(parent depths)` before schema freeze. | insufficient |
| U06 | Exact multiplicity, component, stability, fidelity, and experiment-budget thresholds are intentionally deferred to DS@v5/IP (`.planning/2026-08-01-leaderboard-top-4-loop/SK_v5.md:148-158`). | Engine schema should require versioned contracts but must not invent threshold values in this audit. | insufficient |
| U07 | No dependency/runtime check or test execution was authorized, so SQLite/LangGraph failpoint mechanics and current environment compatibility were not observed. | Root-owned implementation must run the pure contract suite in the project environment and label PASS only for its tested scope. | insufficient |

## One evidence-led DS@v5 recommendation

| Recommendation ID | Source fact | Recommendation | Evidence tag |
|---|---|---|---|
| R01 | The strongest correctness defect is the unavoidable crash window between an authoritative CLOSED event and separately persisted registry mutation (`src/baram/loop/events.py:239-346`; `src/baram/loop/registry.py:121-136`; `src/baram/loop/graph_staged.py:371-425,516-522`). The same split prevents atomic parent closure plus result-child creation. | **Freeze one architectural decision first: make EventStore’s SQLite transaction the sole authoritative node/lineage/lifecycle write path, with `SpecRegistry` as a derived facade/export.** Then implement the hierarchical state machine on that invariant. This single decision simultaneously enables atomic parent/child lineage, idempotent result-conditioned children, restart reconciliation, immutable re-entry revisions, and verifiable pause/success. Do not implement node depth on top of two independently writable stores. | directly_supported |

## Direct DS@v5 implications

| Claim ID | Source fact | DS implication | Evidence tag |
|---|---|---|---|
| P01 | Eight allowed engine modules each contain a specific portion of the old semantics documented above. | The minimum implementation surface is `stages.py`, `research.py`, `registry.py`, `router.py`, `graph_staged.py`, `events.py`, `evaluation.py`, `ontology.py`, the versioned structure-only ontology, and `tests/loop/`; `state.py` requires a separate authority expansion/audit. | directly_supported |
| P02 | Existing evidence/governance tests cover valuable fail-closed behavior, while several tests explicitly codify superseded single-success/exhaustion behavior. | Preserve governance tests, add the executable matrix, and intentionally rewrite only assertions that conflict with SK@v5 pause/reproduction semantics. | directly_supported |
| P03 | The current stage taxonomy is correct but its completion and depth semantics are not (`src/baram/loop/stages.py:37-84,107-150`). | Reuse S1–S11 IDs; version stage snapshots and lineage instead of renaming the pipeline. | directly_supported |
