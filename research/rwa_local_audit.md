# RWA-LOCAL — audit of foundation-to-depth workflow enforcement

**Audit boundary:** read-only review of exactly 35 targeted local files; no web/network use, model fit, score call, project-script execution, lockbox access, or external action. The only repository write is this report.

## Material claims

| ID | Material claim | Evidence tag | Source locator | Scope match | Implication | Proposed delta |
|---|---|---|---|---|---|---|
| C01 | The workflow **documents** a foundation-to-depth story, but the persisted and executable surfaces do **not enforce** descent from S1 through S11. | `contradicts_premise` | `.planning/2026-08-01-leaderboard-top-4-loop/task_plan.md` → “Next Step”; `research/nodes/registry.json` → `$.stages`; `research/engine/graph.json` → `$.nodes,$.edges`; `research/engine/pipeline.py` → `build_app` equivalent is absent here and stage order is not checked | Exact audit question | A stage label or narrative closure can be bypassed without a contract violation. | Make one authoritative stage DAG with machine-checked prerequisites, typed child kinds, closure/revival state, and a mandatory handoff event before any deeper node is admitted. |
| C02 | M270's research pass covered nine scopes, not the requested eleven: purpose and precision analysis were not independent stages, while validation was identified as weak and reprioritised. | `near_match_only` | `reports/m270_p1_research.md` → “스코프별 상태”, “2라운드”, “갱신된 순서” | M270 taxonomy only | M270 supplied useful foundations but cannot itself prove S1–S11 coverage or descent. | Map every M270 claim into explicit S1–S11 IDs, mark gaps, and require the mapping before migration into a later registry. |
| C03 | The later registry contains named entries for every S1–S11 stage and also S12–S16, so nominal stage coverage exists. | `directly_supported` | `research/nodes/registry.json` → `$.stages` | Exact stage inventory | Coverage by name is not the missing piece; enforcement and semantics are. | Retain the inventory but validate it against a stage schema rather than treating key presence as completion. |
| C04 | Stage order is observably non-monotone: the narrative snapshot has S9 in progress and S11 open while S10 and S12 are closed; the authoritative registry later has S14/S15 open while S16 is closed. | `contradicts_premise` | `research/ladder/ladder.json` → `$.authoritative_live_state,$.stages`; `research/nodes/registry.json` → `$.stages.S9_문제해결접근` through `$.stages.S16_밴드적중_조건화` | Exact descent order | Stage numbers currently describe topic/session chronology, not a prerequisite chain. Re-entry may be valid, but it is not distinguished from skipping. | Add `requires`, `invalidates`, `reentry_reason`, and `handoff_to` fields; reject a deeper-stage close unless prerequisites are closed or an explicit re-entry waiver names the overturned premise. |
| C05 | The current persisted excavation graph is a star: all 18 non-root nodes have parent `C000`; there is no foundation-to-depth child lineage, no frontier, and all 18 edges are root-to-child. | `contradicts_premise` | `research/engine/graph.json` → `$.nodes[*].parent,$.edges` | Exact graph semantics | The graph repeats the “all siblings at one rung” failure that `graph.py` says it was meant to replace. | Require parent stage compatibility and at least one causal evidence edge; distinguish `champion_control` from `parent_evidence` so every experiment is not made a semantic child of the champion. |
| C06 | The current `Node`/`admit` contract accepts an arbitrary `stage` string and optional `parent`; it checks neither parent existence nor stage prerequisites, premise lineage, child kind, closure state, or next-node ownership. | `contradicts_premise` | `research/engine/contract.py` → `Node`, `REQUIRED_PREREG`, `admit`; `research/engine/graph.py` → `ExcavationGraph.propose` | Exact admission enforcement | Invalid depth or semantics are representable and admissible. NetworkX will even create a missing parent implicitly when the edge is added. | Add enums for stage/node/evidence kind, require existing parent IDs, validate allowed stage transitions, and reject orphan, backward, or unjustified skip edges. |
| C07 | Registry child semantics are heterogeneous rather than stage-typed: S1–S4 are claims, S5–S7 are treatments, S8 is one gate note, S9 children are objectives/PCA/terrain features, S10 children are blends, and S11 has one inconclusive information-floor diagnostic. | `contradicts_premise` | `research/nodes/registry.json` → `$.stages.S1_목적파악.nodes` through `$.stages.S11_분석방법적절성.nodes` | Exact child-node semantics | An `S9-*` or `S10-*` prefix does not establish that the node evaluates workflow rather than another model/feature candidate. | Define allowed child kinds per stage and separate `finding`, `candidate`, `gate`, `workflow_rule`, `audit`, and `handoff` records. |
| C08 | The pipeline decomposition is a promising finer-grained depth model (22 stages, reverse-ablation contributions), but its persisted spec has 17 `unresearched`, 4 `built`, and 1 `ablated` stages and is not connected to the S1–S11 registry or graph admission. | `near_match_only` | `research/engine/pipeline.py` → `STAGES,CLUSTERS`; `research/engine/pipeline_spec.json` → `$.stages` | Partial architectural match | It improves decomposition but currently forms a third, disconnected source of workflow state. | Make pipeline stages children of the relevant S5–S10 nodes and derive both registry summaries and graph nodes from one event store. |
| C09 | Legacy M271 state reducers do enforce deterministic in-memory merge semantics for write-once records and owned deficit cells. | `directly_supported` | `.planning/2026-08-01-leaderboard-top-4-loop/m271_state.py` → `write_once`, `owned_cells`, `best_score`, `M271State` | Exact parallel-state merge scope | Concurrent branch reduction has useful guards, but this is not disk-level transactionality or whole-cycle atomicity. | Preserve these reducers as validation inside a transactional commit rather than treating reducer determinism as atomic persistence. |
| C10 | Neither graph nor ledger persistence is atomic: graph state is written with `json.dump(open(path,'w'))`; ledger `record()` performs read–append–full-file `write_text()` without lock, CAS, temp-file replace, fsync, or a transaction spanning receipt/graph/registry/champion. | `contradicts_premise` | `research/engine/graph.py` → `save`; `.planning/2026-08-01-leaderboard-top-4-loop/m271_ledger_state.py` → `load_history`, `_save`, `record`; `.planning/2026-08-01-leaderboard-top-4-loop/m271_cycle_contract.py` → `close_cycle` | Exact atomic-ledger question | A crash can truncate state and concurrent writers can lose events; “append-only” is logical, not physical. Related projections can diverge. | Use a single SQLite transaction or locked append-only JSONL/WAL event; include receipt digest, graph delta, cell delta, champion, comparison count, and next-node handoff in one commit, then derive JSON/Markdown views. |
| C11 | The cycle contract was not actually integrated: after it was introduced, the close report still shows 88 unrecorded cycles, zero individual records, and unchanged history while simultaneously claiming the post-contract-recording guard passed. | `contradicts_premise` | `reports/m271_close_cycles.md` → §§1–5; `reports/m271_ledger_contract.md` → §§2–4; `reports/m271_p4_route_receipt.json` → `$.context.ledger_contract.unrecorded_cycles` | Exact ledger-completeness scope | “API exists” and “contract enforced” are not equivalent; historical routing and stall/mass calculations are incomplete. | Make `close_cycle` the only way to transition a node out of `run`; fail graph save and next routing when the same transaction lacks the ledger event. |
| C12 | Premise-aware closure/revival exists as a **manual evidentiary practice**: M270 classified closure premises conservatively, S3-E overturned a prior bottleneck claim, and S13 explicitly re-entered upstream stages. | `directly_supported` | `reports/m270_revived_lanes.md` → §§2–5; `research/nodes/registry.json` → `$.stages.S3_데이터정밀분석.nodes[S3-E]`, `$.stages.S13_사다리재진입_S5S6` | Exact premise/re-entry concept | The team can correct a false closure, which is a strong semantic foundation. | Promote premise IDs, supporting predicates, and invalidation edges from prose into the authoritative graph/event schema. |
| C13 | Revival is not enforced in the current app: C9 `REVIVE` is routed straight to `JOIN`, with no checkpoint fork, premise mutation, descendant reopening, or persisted graph change. `REFINE_AXIS` is also a no-op join. | `contradicts_premise` | `.planning/2026-08-01-leaderboard-top-4-loop/m271_app.py` → `route_after_gate`, `build_app`; `reports/m271_framework_gate.md` → condition 4e (capability demo only) | Exact closure/revival execution | The framework can fork a checkpoint, but the production routing path does not use that capability. | Implement state-mutating operators for `REVIVE`, `CLOSE_AXIS`, and `REFINE_AXIS`; test that descendants change status and frontier, not merely that a demo graph can fork. |
| C14 | Closure labels are not state-safe: cycle 47 is titled “external NWP revived,” but its final verdict is `STILL_CLOSED_UNDER_CORRECTED_REQUIREMENT` and `flipped premise=False`. | `contradicts_premise` | `reports/m271_cycle47_external_nwp_revived.md` → title, §3 verdict; `reports/m271_cycle46_closure_falsification.md` → §5 | Exact premise/revival bookkeeping | A filename/node name can assert revival even when the decision says no revival, making lineage searches unsafe. | Generate node titles/status from the committed decision, and add an invariant that `REVIVED` names require a persisted false→true premise transition. |
| C15 | M271 v3 historically added a magnitude gate (C16) and a coarse repetition/novelty gate (C17), and P4 receipts show C16 routing; however C17 is metadata repetition, not semantic novelty, and the 0.15-gap threshold is explicitly uncalibrated. | `near_match_only` | `reports/m271_c10_loop_engine.md` → §§2–6; `reports/m271_p4_route.md` → §§1–3 | Historical novelty/magnitude routing | The intended routing dimensions exist, but only as heuristic, generation-specific logic. | Store a versioned proposal fingerprint plus mechanism/inputs/estimand differences; calibrate or at least sensitivity-audit the magnitude threshold, and preserve the decision in the common event ledger. |
| C16 | Router lineage is fragmented: current `m271_app.py` imports `m271_router.decide`, whose frozen version is v2 and has no C16/C17, while the P4 report/receipt claims v3. | `contradicts_premise` | `.planning/2026-08-01-leaderboard-top-4-loop/m271_router.py` → `ROUTER_VERSION`, `CONDITIONS`, `decide`; `.planning/2026-08-01-leaderboard-top-4-loop/m271_app.py` → import; `reports/m271_p4_route_receipt.json` → `$.router_version` | Exact active-routing identity | A report cannot prove what the current executable router will enforce. | Persist the router source digest with every decision and refuse replay when the imported router digest/version differs. |
| C17 | Several v2 evidence semantics cannot fire through the app because `route_after_gate` drops `axis_kind`, `negative_finding`, and `seeds_cells` when constructing `Evidence`; therefore C12/C13/C14/C15 are unreachable through that path. | `contradicts_premise` | `.planning/2026-08-01-leaderboard-top-4-loop/m271_router.py` → `Evidence`, C12–C15; `.planning/2026-08-01-leaderboard-top-4-loop/m271_app.py` → `route_after_gate` | Exact router-to-app contract | Novel axes, structural consequences, measured-negative closure, and ledger-seeded fanout can silently disappear at the adapter boundary. | Replace manual field copying with validated `Evidence(**raw)`/Pydantic parsing and contract tests that make every condition fire end-to-end. |
| C18 | The current engine does not enforce declared magnitude: admission merely requires a `min_effect` key, and `loop.cycle` calls `arbitrate` without passing that node value, so the global default is used instead. | `contradicts_premise` | `research/engine/contract.py` → `REQUIRED_PREREG`, `admit`; `research/engine/loop.py` → `cycle`; `research/engine/arbiter.py` → `arbitrate` | Exact magnitude gate | A predeclared threshold can be recorded but ignored, breaking preregistration semantics. | Validate `min_effect >=` the stage floor and pass the exact declared value into arbitration; persist both declared and applied values and assert equality. |
| C19 | Oracle-versus-deployable separation is handled well in several reports: shrink-to-truth is labelled an unattainable ruler, oracle offset transfer is caveated, leaky and clean oracles are distinguished, and in-sample saved-prediction traps are rejected. | `directly_supported` | `reports/m270_revised_verdicts.md` → §§1,3; `research/nodes/registry.json` → `S3-A/S3-B`; `reports/s12_session_report.md` → §§2–3 | Exact evidence-interpretation practice | The narrative analysis usually avoids presenting oracle ceilings as candidates. | Preserve these distinctions in typed evidence rather than relying on report prose. |
| C20 | Oracle/deployability is not schema-enforced: `Node.result` is an arbitrary dictionary; admission requires no evidence class or basis-time receipt, and recent-observation refusal works only when the proposer truthfully sets an optional flag. The accepted-gain ledger also retains screening/provisional/isolated findings beside deployable gains. | `near_match_only` | `research/engine/contract.py` → `Node`, `admit`; `research/nodes/registry.json` → `$.accepted_gain_ledger` | Exact oracle/deployable enforcement | Downstream consumers can mistake a diagnostic, isolated delta, or screening result for a promotion-ready gain. | Require `evidence_class={oracle,diagnostic,screening,foldoutside,deployable}`, `availability_proof`, and `promotion_eligible`; permit only deployable/fold-outside records to update champion or accepted gain. |
| C21 | Multiplicity adjustment is implemented in the arbiter: it raises the required posterior with comparison count and also requires a minimum point effect. | `directly_supported` | `research/engine/arbiter.py` → `arbitrate`; `research/engine/contract.py` → R6 | Exact per-call multiplicity method | The statistical rule itself is present and auditable. | Keep the formula, but source its count from a durable, scope-defined comparison family. |
| C22 | Multiplicity is not active in persisted history: `graph.json` has `comparisons=0` and empty arbitration objects despite 15 non-refused evaluated nodes, while the contract acknowledges roughly 30 earlier treatments. The family bandit is also not persisted in graph state. | `contradicts_premise` | `research/engine/graph.json` → `$.comparisons,$.nodes[*].status,$.nodes[*].arbitration`; `research/engine/graph.py` → `record_arbitration`; `research/engine/select.py` → `FamilyBandit`; `research/engine/contract.py` → R6 | Exact multiplicity across workflow | Restart/manual insertion resets the adjustment and exploration posterior, so the best-of-many risk is understated. | Backfill an immutable comparison-family ledger from admissible receipts; require every evaluated node to carry an arbitration event, and persist bandit sufficient statistics. |
| C23 | The current app does not implement a full next-node handoff: its connected path is `MEASURE → GATE → RESEARCH_DIRECTION/PRUNE/JOIN → END`; registered `RESEARCH_SOTA`, `MATERIALIZE`, and `EXPERIMENT` operators have no incoming path. A `NONE/HALT` decision says “choose the next frontier node,” but the app simply joins and ends. | `contradicts_premise` | `.planning/2026-08-01-leaderboard-top-4-loop/m271_app.py` → `build_app`, `route_after_gate`; `.planning/2026-08-01-leaderboard-top-4-loop/m271_router.py` → `decide` no-fire branch | Exact next-node handoff | Direction research does not mechanically descend to SOTA, materialisation, experiment, gate, ledger update, and successor selection. | Wire and test the complete lifecycle; the terminal event must contain exactly one next node, a justified fanout, or a typed terminal reason. |
| C24 | P4 decision semantics and engine events disagree: C7 `scale_up`, C16 rejects, and a termination halt all produced `RESEARCH_DIRECTION` events/sources of kind `loop_engine` in L8. | `contradicts_premise` | `reports/m271_p4_route_receipt.json` → each `$.decisions[*].condition/action/targets/engine_events/sources_opened`; `reports/m271_p4_route.md` → §§2–4 | Exact router-to-child handoff | The logged child is not a faithful materialisation of the router decision, so next-node provenance is unreliable. | Assert action-to-event invariants: reject/halt creates no research source; C7 inherits its lane/kind; every source digest must equal the selected target digest. |
| C25 | There is no single authoritative next node: the persisted graph frontier is empty, the registry has open stages, the ladder explicitly admits it can lag, and `queue.txt` contains two free-form jobs without stage, parent, premise, or gate metadata. | `insufficient` | `research/engine/graph.json` → frontier statuses; `research/nodes/registry.json` → open stages; `research/ladder/ladder.json` → `$.authoritative_live_state`; `research/nodes/queue.txt` | Exact next-node identity | The audit cannot derive one deterministic next handoff from existing state. | Replace the free-form queue with a derived frontier view from committed graph events and reject multiple owners unless the router emitted an explicit fanout. |
| C26 | S12–S16 contain valuable diagnosis and explicit upstream re-entry, but their numbering is session chronology rather than enforced depth: S12/S13/S16 are closed while S11 and S14/S15 remain open. | `near_match_only` | `research/nodes/registry.json` → `$.stages.S11_분석방법적절성` through `$.stages.S16_밴드적중_조건화`; `reports/s12_session_report.md` | S12–S16 relation to S1–S11 | Later sessions can legitimately revise foundations, but they cannot serve as proof that the original descent completed. | Represent S12–S16 as audit/re-entry nodes attached to the premise or stage they revise, not as deeper rungs in the same ordinal ladder. |

## S1–S11 coverage matrix

| Stage | Coverage claim | Evidence tag | Source locator | Scope match | Implication | Proposed delta |
|---|---|---|---|---|---|---|
| S1 — purpose | Metric algebra, target, and “prediction is a settlement action, not a conditional mean” are explicitly recorded in two confirmed nodes. | `directly_supported` | `research/nodes/registry.json` → `$.stages.S1_목적파악.nodes`; `reports/m270_p1_research.md` → finding 1 | Exact content; declarative only | Foundation content is strong, but the stage has no explicit status/closure or handoff. | Add a signed S1 closure with downstream invariants consumed by S4/S7/S9. |
| S2 — data traits | Turbine/group mapping, SCADA validity, unusable columns, and one-issuance structure are explicitly covered in five nodes. | `directly_supported` | `research/nodes/registry.json` → `$.stages.S2_데이터특성.nodes` | Exact content; declarative only | Core data constraints exist, including an axis-closing fact, but are not admission predicates. | Convert each trait into a tested data-contract predicate and attach affected downstream axes. |
| S3 — precision analysis | Clean/leaky oracles, physics ceiling, irrecoverable availability loss, and an overturned bottleneck premise are explicitly measured. | `directly_supported` | `research/nodes/registry.json` → `$.stages.S3_데이터정밀분석.nodes`; `research/ladder/ladder.json` → `$.premise_correction` | Exact content and causal diagnosis | This is the strongest foundation rung and demonstrates real premise correction. | Make S3 evidence IDs mandatory parents of S5–S7 hypotheses and of any closure revival. |
| S4 — metric | Pooled single-policy protocol, loss concentration, decision-layer ceiling, and zero-contribution rows are covered. | `directly_supported` | `research/nodes/registry.json` → `$.stages.S4_평가지표이해.nodes`; `reports/s12_session_report.md` → §1.1 | Exact content; no stage gate | Metric understanding is substantive, but later node admission does not prove it uses the same policy surface. | Require metric/policy/protocol digest inheritance on every score-bearing child. |
| S5 — preprocessing | Twenty-three treatments plus a closure record identify an accepted availability gate, soft cap, production weight, and fold-outside gain. | `directly_supported` | `research/nodes/registry.json` → `$.stages.S5_데이터전처리.nodes,$.closure` | Strong treatment coverage; heterogeneous schema | S5 has an outcome, but its nodes do not conform to the later preregistration contract and the stage lacks an explicit `status`. | Normalize historical nodes into typed treatment/control/gate records and emit a formal S5→S6 handoff. |
| S6 — features | Six feature nodes and a `CLOSED` status exist, but the accepted G2 entry was originally screening-only and the registry itself records a later fold-outside sign reversal before retaining it for blend diversity. | `near_match_only` | `research/nodes/registry.json` → `$.stages.S6_피처구성`, `$.accepted_gain_ledger[stage=S6]` | Feature scope covered; closure confidence unresolved | “Closed” and “accepted” are not a clean final state for the same evidence lineage. | Split point-skill and diversity estimands; rejudge/close each under its declared deployable architecture and update one verdict. |
| S7 — modelling | Distribution, policy-grid, pooling, conditioning, and multiple model-family nodes are covered with a `CLOSED` status. | `directly_supported` | `research/nodes/registry.json` → `$.stages.S7_모델링` | Exact modelling scope | Rich modelling coverage exists, but dependency on S1–S6 is narrative rather than graph-validated. | Require every S7 node to inherit S4 metric digest, S5 treatment digest, and S6 feature-set digest. |
| S8 — validation | One fold-outside gate node is applied, but M270 recommended monthly rolling-origin validation and S12 still records the missing-winter/Q1 defect as unresolved. | `near_match_only` | `research/nodes/registry.json` → `$.stages.S8_검증전략`; `reports/m270_p1_research.md` → finding 3; `reports/s12_session_report.md` → §5 | Partial validation scope | Selection leakage is addressed, but seasonal representativeness and multiplicity are not closed at the stage level. | Add the declared monthly/Q1 seasonal protocol or record a justified waiver; bind multiplicity family/count to S8. |
| S9 — problem-solving workflow | The stage exists, but its seven children are mainly loss/objective and feature candidates rather than workflow rules; its status is a candidate-list result. | `contradicts_premise` | `research/nodes/registry.json` → `$.stages.S9_문제해결접근` | Semantic mismatch | Technical experiments were relabelled as workflow analysis, so S9 does not establish a problem-solving process contract. | Move technical candidates to S6/S7 and populate S9 with diagnosis→hypothesis→research→predeclare→execute→gate→update rules and end-to-end tests. |
| S10 — improvement workflow | Four nodes demonstrate member/blend improvement, but they cover one improvement tactic rather than a general recursive improvement workflow. | `near_match_only` | `research/nodes/registry.json` → `$.stages.S10_모델개선전략`; `research/engine/select.py` | Adjacent outcome, incomplete workflow semantics | A local gain was found, yet novelty, magnitude, budget, closure, and next-node selection are not unified here. | Define S10 as the versioned improvement router/portfolio policy; keep blends as S7 children or S10 outputs, not the workflow definition itself. |
| S11 — appropriateness | The only node is an inconclusive, radius-dominated kNN information-floor diagnostic and the stage remains open. | `insufficient` | `research/nodes/registry.json` → `$.stages.S11_분석방법적절성`; `research/ladder/ladder.json` → S11 | Exact stage, inadequate evidence | No final audit establishes that methods, assumptions, evidence class, multiplicity, and deployment constraints are jointly appropriate. | Add an S11 checklist/gate over causal premise, chronology, deployability, oracle separation, multiplicity, robustness, provenance, and reproducibility; close only after all typed checks pass. |

## Three highest-value workflow defects

| Rank | Defect | Evidence tag | Source locator | Scope match | Implication | Proposed delta |
|---:|---|---|---|---|---|---|
| 1 | **No executable descent or faithful child handoff.** The live graph is a root-star, stage/parent semantics are unchecked, the app skips SOTA/materialisation/experiment, and P4 events do not match router actions. | `contradicts_premise` | `research/engine/graph.json`; `research/engine/contract.py` → `admit`; `.planning/2026-08-01-leaderboard-top-4-loop/m271_app.py` → `build_app`; `reports/m271_p4_route_receipt.json` | Exact foundation-to-depth and next-node scope | This is the primary reason the workflow can look deep in reports while executing as disconnected siblings. | Build one typed lifecycle DAG: `foundation finding → direction → SOTA → candidate → prereg → experiment → gate → ledger/closure → handoff`; enforce parent/stage/action invariants before commit. |
| 2 | **No atomic, authoritative cycle ledger.** Direct JSON rewrites coexist with registry, ladder, graph, queue, and pipeline spec; 88 cycles remained unrecorded after “enforcement.” | `contradicts_premise` | `.planning/2026-08-01-leaderboard-top-4-loop/m271_ledger_state.py`; `research/engine/graph.py` → `save`; `reports/m271_close_cycles.md`; `research/ladder/ladder.json` → `$.authoritative_live_state` | Exact atomic-ledger scope | Closure mass, champion, multiplicity, frontier, and stage status can disagree or be lost on crash. | Adopt a transactional event store as the sole writer; atomically commit receipt digest, result class, graph/ledger mutations, comparison count, router digest, and next handoff; generate every other file as a read-only projection. |
| 3 | **Critical decision gates are recorded but bypassable or unreachable.** Revival/refinement are no-op joins, several Evidence fields are dropped, declared `min_effect` is ignored, oracle/deployability is untyped, and persisted multiplicity remains zero. | `contradicts_premise` | `.planning/2026-08-01-leaderboard-top-4-loop/m271_app.py` → `route_after_gate`; `.planning/2026-08-01-leaderboard-top-4-loop/m271_router.py` → `Evidence,C12–C15`; `research/engine/loop.py`; `research/engine/arbiter.py`; `research/engine/graph.json` | Exact closure, routing, oracle, magnitude, and multiplicity scope | Even a correctly labelled node can evade the intended safeguards at adapter, restart, or manual-import boundaries. | Introduce one validated evidence schema, end-to-end branch tests for every router action, typed result classes/availability proofs, exact declared-threshold application, and durable comparison-family/bandit state. |
