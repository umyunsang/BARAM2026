# 적대적 리뷰 — M271 발굴 루프 (2026-08-06, 외부 세션)

리뷰 대상: `~/baram2026` 전체 · 발굴그래프 136노드 · 리포트 303건 · 온라인 앵커 2건
리뷰 방식: CLI 실행(`audit` 통과) · 그래프/리포트 판독 · 독립 지표 시뮬레이션 대조

## A1. 정체의 원인은 아이디어 고갈이 아니라 **판정 불능**이다  [directly_supported]
`progress.md`(08-04) 는 "no concrete unopened lane remains" 로 닫혔지만, 08-06 그래프에는 **LIVE 71개**가 있고
그중 최소 3개가 측정된 양의 효과를 가진 채 **미판정**이다. 정체 카운터 97 은 탐색 고갈이 아니라
(a) 독립 검증면 0, (b) 실험틀 결손으로 인한 V1 가드 발화, (c) 작은 참효과를 거르는 월별 게이트
— 세 가지 **판정 장치의 포화**를 가리킨다. 루프 재설계의 1번 과제는 노드 추가가 아니라 판정 능력 복구다.

## A2. `C1N41` 차단 원인은 이미 특정되어 있고 해소 가능하다  [directly_supported]  ★최고가치
- BAND(정산모양 soft target, 46클래스) 처리효과: 사이클40 `+0.009772` → 사이클41 `+0.021490` (부호 재현 H5=True)
- 미판정 사유: V1 가드 — CONTROL 0.567875 vs 배포 0.628605 = **-0.060730** (허용 -0.03)
- 결손 원인이 리포트에 명시됨: **`sitewind__*` teacher 피처 13개 결측**
- 즉 "가설이 틀려서"가 아니라 **실험틀이 배포 수준에 못 미쳐서** 판정을 못 했다.
- 처리효과 `+0.021490` 은 rank-20 잔여 격차 `+0.023473` 과 사실상 같은 크기다.
→ **teacher 피처 복원 → V1 통과 → 판정**이 남은 최단 경로.

## A3. 결정층이 **로컬 표면에서 튜닝되어 온라인과 미스매치**일 가능성 [near_match_only → 검증 필요]
- 앵커가 말하는 것: online FICR − local FICR = **+0.012027**(M261) / **+0.037267**(M252). 1-NMAE 는 거의 불변.
  즉 온라인 오차분포는 로컬보다 **더 tight** 하다.
- 독립 시뮬(본 세션 L0): 결정층이 가정하는 산포가 실제보다 **크면** 손해가 급격하다.
  (실제 16% 기준: 가정13% → 0.66861 / 가정16% → 0.66191 / 가정19% → 0.65295 / 가정22% → 0.63912)
- 배포 정책은 `M269@T0.5_G1.5` 로 **로컬 표면에서** 온도·게인이 고정됐다.
- `m270_revised_verdicts.md` 는 k-축소 표면에서 **재채점**만 했고 **정책 재최적화**는 하지 않았다.
→ 미수행 실험: **온라인 등가(k≈0.89) 표면에서 T/G 재최적화**. 적합 0회, 파생 전용, 저비용.
   `C1N44_SHARPENED_DECISION`(BAND_TARGET_AND_TEMPERATURE_ARE_SUBSTITUTES)은 이 질문을 닫지 않는다 —
   그것은 두 손잡이의 대체관계이지, 표면 이동에 따른 최적점 이동이 아니다.

## A4. `C1N90` 은 가설이 아니라 **정정**인데 탐색용 게이트로 기각됐다  [directly_supported]
`bayes_decision` 이 46개 구간을 중심점 점질량으로 취급 — 구간폭 0.02 vs FICR 창 ±0.06.
uniform 적분으로 고치면 `+0.001890` (검출문턱 0.001013 초과, H4 FICR 우세 확인).
월별 게이트는 **경험적 탐색의 과적합을 막는 장치**인데, 수치적분 오류 정정에 그대로 적용됐다.
→ 게이트 적용 범주를 분리할 것: `CORRECTION`(이론적 정합성) vs `HYPOTHESIS`(경험적 탐색).

## A5. 진단 재조정 — 격차는 더 이상 "FICR 93%" 가 아니다  [directly_supported]
저장된 리더보드 HTML(08-01)은 stale 이다. 현재 최고 온라인은 M261 `0.6365274`.
| | 1-NMAE | FICR | Total |
|---|---:|---:|---:|
| M261 (현재) | 0.8578854 | 0.4151695 | 0.6365274 |
| rank-20 스냅샷 | 0.87991 | 0.43952 | 0.659715 |
| 격차 | **+0.02202 (47%)** | **+0.02435 (53%)** | +0.02347 |
→ 이제 **1-NMAE 와 FICR 이 거의 반반**이다. FICR 단일 공략 전략은 갱신되어야 한다.
   유용한 항등식: 목표 Total 0.66 에서 **필요 FICR = 1.32 − (우리 1-NMAE)**.

## A6. 위생 — `.planning` 에 `src` 밖 일회성 스크립트 283개  [directly_supported]
`build_*.py` 계열이 저장소 모듈 계약 밖에서 산출물을 만든다. 감사·재현·이관 리스크.
→ 승격 규칙 필요: 2회 이상 재사용된 스크립트는 `src/baram/` 모듈로 승격, 나머지는 아카이브.

## 권고 우선순위
1. **P0** teacher 피처(`sitewind__*`) 복원 → `C1N41` 판정 (적합 필요, root 전용)
2. **P0** 온라인 등가 표면에서 결정층 T/G 재최적화 (적합 0회, 즉시 가능)
3. **P1** 게이트 범주 분리(CORRECTION/HYPOTHESIS) 후 `C1N90` uniform 적분 재판정
4. **P1** 판정 능력 복구: Ladder η-게이트(arXiv:1502.04585) 도입 — 검증면 소진 상황의 표준 처방
5. **P2** LangGraph 이식: 위 판정 장치를 노드/엣지로 고정하고 136노드 상태를 그대로 적재
