# RWA-LOOP — scientific-discovery / experiment-graph operation semantics

## 0. Bound and evidence protocol

- 이 레인은 외부 자료 **7개**만 사용했고, 모두 원 논문·공식 학회 논문·공식 논문 호스팅 면이다; 검색·열람일은 `2026-08-08`이다. **directly_supported — R1–R7 source register below; 2026-08-08; scope=source-count/retrieval audit.**
- 로컬 비교면은 `M271_ROUTER_v3_frozen_2026-08-06`, 원장 계약/감사 receipt, `S12-N21_workflow_audit.json`으로 한정하며 외부 7개 한도에는 넣지 않는다. **directly_supported — L1–L6 locators below; 2026-08-08; scope=current-workflow comparison only.**
- 각 evidentiary claim 행에는 허용된 증거 태그를 하나만 붙인다. **directly_supported — `.planning/2026-08-01-leaderboard-top-4-loop/SK_v2.md:86-88`; 2026-08-08; scope=project evidence protocol.**
- 결론은 **M271/LangGraph의 교체가 아니라 실행 계약의 보강**이다; 조사한 자료는 BARAM의 데이터·점수면에서 새 프레임워크 전체가 우월하다는 증거를 주지 않는다. **insufficient — R1–R7 collectively; 2026-08-08; scope=no head-to-head on BARAM, so no branding/framework replacement claim.**

## 1. Source register (7 / maximum 8)

| ID | Primary / official source | Exact locator used | Evidence |
|---|---|---|---|
| R1 | W. M. P. van der Aalst, *The Application of Petri Nets to Workflow Management* (1998), [PDF](https://users.cs.northwestern.edu/~robby/courses/395-495-2017-winter/Van%20Der%20Aalst%201998%20The%20Application%20of%20Petri%20Nets%20to%20Workflow%20Management.pdf) | Def. 6 PDF p.17; Def. 7 p.34; Thm. 1 p.37 | **directly_supported — cited definitions/theorem; 2026-08-08; scope=workflow reachability, completion, and dead-transition semantics.** |
| R2 | Yamada et al., *The AI Scientist-v2* (arXiv:2504.08066v1, 2025), [paper](https://arxiv.org/pdf/2504.08066) | Fig. 1 p.3; §§3.2.1–3.2.2 pp.5–7; Table 3 p.21 | **directly_supported — cited pages; 2026-08-08; scope=staged experiment tree, checkpoints, replications, and fixed budgets.** |
| R3 | Huang et al., *Automated Hypothesis Validation with Agentic Sequential Falsifications* (POPPER, arXiv:2502.09858v1, 2025), [paper](https://arxiv.org/pdf/2502.09858) | §§2.1–2.4 pp.2–5; Algorithm 1 / proof p.13; limitations p.15; failure taxonomy p.16 | **directly_supported — cited pages; 2026-08-08; scope=falsification contract and anytime-valid sequential evidence under stated assumptions.** |
| R4 | Lange, Imajuku & Cetin, *ShinkaEvolve* (ICLR 2026 official proceedings), [paper](https://proceedings.iclr.cc/paper_files/paper/2026/file/7886b9bafe76c52fd568db10ff9772df-Paper-Conference.pdf) | §§3.1–3.2 pp.3–4; ablation §§5–6 pp.8–9; hyperparameters p.17 | **directly_supported — official proceedings locators; 2026-08-08; scope=archive-aware novelty and adaptive search in program evolution.** |
| R5 | Xie et al., *Cost-aware Stopping for Bayesian Optimization* (arXiv:2507.12453v5 / PMLR 306, 2026), [paper](https://arxiv.org/pdf/2507.12453) | §§2.1–3 pp.2–6; Eq. 7 and explanation p.4; Cor. 3.3/3.5 pp.5–6,14–15 | **directly_supported — cited version/pages; 2026-08-08; scope=matched acquisition/stopping with uncertainty and evaluation cost.** |
| R6 | J. de Kleer, *An Assumption-based TMS*, Artificial Intelligence 28 (1986), [author-hosted paper](https://www.dekleer.org/Publications/An%20Assumption-Based%20TMS.pdf) | §§1.1, 4.3–4.4, 4.9; journal pp.129–130, 144–147, 153 (PDF pp.3–4,18–21,27) | **directly_supported — cited sections/pages; 2026-08-08; scope=justification/assumption dependency and belief re-entry.** |
| R7 | Huber et al., *AiiDA 1.0, a scalable computational infrastructure for automated reproducible workflows and data provenance* (Scientific Data 7:300, 2020), [primary article](https://pmc.ncbi.nlm.nih.gov/articles/PMC7479590/) | “Engine and Workflow Language”; “The Provenance Model”; “Node types”; “Reproducibility and efficiency” | **directly_supported — named article sections; 2026-08-08; scope=automatic process provenance, typed edges, immutability, and restartable workflows.** |

### Local comparison locators

| ID | Locator | Comparison surface | Evidence |
|---|---|---|---|
| L1 | `.planning/2026-08-01-leaderboard-top-4-loop/m271_router.py:71-102,169-241,269-312,352-432` | `Evidence`; C2–C15; C11 value/selection | **directly_supported — exact local lines; 2026-08-08; scope=M271 router v2 base used by v3.** |
| L2 | `.planning/2026-08-01-leaderboard-top-4-loop/m271_c10_loop_engine.py:111-114,148-246` | v3 C16, C17, one-shot C10, termination guard | **directly_supported — exact local lines; 2026-08-08; scope=M271 router v3 wrapper.** |
| L3 | `.planning/2026-08-01-leaderboard-top-4-loop/m271_cycle_contract.py:48-129` | optional `close_cycle`, audit backfill, `require_recorded` | **directly_supported — exact local lines; 2026-08-08; scope=cycle-record enforcement API.** |
| L4 | `.planning/2026-08-01-leaderboard-top-4-loop/m271_ledger_state.py:101-138,183-222` | append history, stall reduction, missing-cycle set | **directly_supported — exact local lines; 2026-08-08; scope=ledger state reducer.** |
| L5 | `reports/m271_close_cycles_receipt.json:6-64` | before/after: `unrecorded_cycles=88`, `stall_counter=97`, `recorded_nodes=[]` | **directly_supported — exact receipt fields; 2026-08-08; scope=observed ledger failure, not reconstructed per-cycle history.** |
| L6 | `research/nodes/S12-N21_workflow_audit.json:2-81` | deep research only S3/S5/S6/S9; S11 open | **directly_supported — exact audit rows; 2026-08-08; scope=observed S1–S11 coverage.** |

## 2. Operation-semantics comparison to M271 router v3

| Required behavior | Primary-source operation semantics | M271 v3 operation semantics | Finding | Evidence |
|---|---|---|---|---|
| Stage coverage | A WF-net requires every task/condition to lie on an input-to-output path; soundness additionally requires completion from every reachable state, proper completion, and no dead transitions. AI Scientist-v2 separately makes four experiment stages explicit, gives each an exit rule, checkpoints the selected node, and runs replications. | `Evidence` has `lane` but no `stage`; C6 detects starved **lanes**, and `HALT` does not inspect S1–S11 coverage. The local audit has dedicated deep research at only S3/S5/S6/S9 and leaves S11 open. | The graph machinery is real, but stage coverage is not an invariant; “node exists” and “stage was descended/researched” are currently conflated. | **directly_supported — R1 Def.6/7 pp.17,34 + R2 §3.2.1 pp.5–6 + L1 lines71-99,224-241 + L6; 2026-08-08; scope=structural workflow comparison, not a claim that Petri nets solve BARAM.** |
| Falsification | POPPER admits a measurable implication only with explicit null/alternative definitions, requires the test to be logically relevant to the parent hypothesis, records failed attempts, and distinguishes non-decisive evidence from rejection. | C2 reads a caller-supplied `refutes_mechanism`; C5 compares two signs; C14 reads `negative_finding`. No router-level schema requires a refuter, null/control, implication edge, decision threshold, or `INCONCLUSIVE` outcome. | C2/C5/C14 are useful routing hooks, but they do not enforce falsifiability or prove that a negative result actually tests the claimed mechanism. | **directly_supported — R3 pp.2,4–5,16 + L1 lines80-99,169-180,212-221,352-368; 2026-08-08; scope=contract-shape comparison; POPPER’s p-value machinery is not assumed applicable to the reused OOF surface.** |
| Novelty acquisition | ShinkaEvolve keeps an evaluated archive; it embeds the mutable program, compares cosine similarity to the archive, and, above a threshold (example `0.95`), asks a second model whether the proposal is meaningfully different. Its parent sampling also uses fitness and offspring count. | C17 rejects when the exact `(lane, proposed_kind)` pair appears at least 3 times in the last 6 entries; treatment, mechanism, data, metric, topology, and parent lineage are not compared. | C17 is a repetition throttle, not semantic novelty: a relabelled duplicate can pass, while three genuinely different mechanisms in one lane/kind can be blocked. | **contradicts_premise — R4 §§3.1–3.2 pp.3–4 + L2 lines111-114,160-166,218-237; 2026-08-08; scope=the premise that C17 operationalizes ShinkaEvolve-style novelty; program embeddings themselves are only a near analogue.** |
| Magnitude/cost acquisition | Cost-aware BO forms a posterior over improvement and matches the acquisition rule to a cost-aware stop rule; its interpretation is to stop when no unevaluated point has positive expected gain relative to cost. | C16 compares one uncalibrated point field `expected_gain` with `0.15 * remaining_gap`; C11 adds `expected_gain / expected_hours` to fixed `base_value`, `voi`, and `exploration` constants. No uncertainty or evidence basis is carried. | C16 is a deterministic magnitude screen, not a calibrated acquisition function; the same acquisition quantity is not used to justify stopping. | **contradicts_premise — R5 Eq.7/§3 pp.4–6 + L1 lines97-99,400-403 + L2 lines111-114,148-157; 2026-08-08; scope=the premise that C16 is normalized novelty/magnitude acquisition; BO guarantees do not transfer without a valid posterior/cost model.** |
| Closure premise invalidation / revival | ATMS associates each conclusion with the assumption environments and justifications under which it holds; a node can move out when its contexts become inconsistent and back in when a later consistent context supports it, while direct retraction recursively recomputes consequents. | C14 closes with the generic premise string `MEASURED_NEGATIVE`; C9 fires on a global `state["flipped_premises"]` list and returns premise names, but no closure record declares the exact premise IDs, supporting evidence, or dependent descendant set. | C9 is a manual revival hook, not dependency-driven invalidation/revival; it cannot deterministically answer which closures a new fact overturns. | **directly_supported — R6 §§4.3–4.4/4.9 pp.144–147,153 + L1 lines281-290,352-365; 2026-08-08; scope=dependency semantics only, not adoption of a full ATMS.** |
| Evidence provenance | AiiDA records calculations/workflows and their inputs/outputs **during execution**, distinguishes data from logical provenance, uses typed/cardinality-checked links, and makes stored content immutable because mutation would invalidate descendants. | M271’s router input has IDs but no receipt hash, code/config/data identity, policy, row key, or source locator. `close_cycle()` is a separate caller action after a receipt exists. The audit still reports 88 missing cycles and no newly recorded nodes. | Optional after-the-fact instrumentation is the demonstrated failure mode; provenance must be owned by the execution wrapper and be the source of graph state, not an optional final line in each script. | **directly_supported — R7 named sections + L1 lines71-99 + L3 lines48-75 + L5 fields `before/after/recorded_nodes`; 2026-08-08; scope=computational provenance and the observed 88-cycle gap.** |
| Stopping under adaptive search | POPPER combines conditionally valid per-test evidence into an e-process, permits optional stopping under its filtration assumptions, and still has a maximum-test budget. Cost-aware BO uses a stop rule matched to the same value/cost model used for acquisition. | M271 halts after C10 was serviced once, stall persists, and no other condition fires; the observed stall input remained 97 while 88 cycles were missing. It has neither a global comparison/evidence budget nor a stage-completeness precondition. | The current stop is mechanically deterministic but epistemically under-specified: stale ledger state can drive it, and repeated adaptive looks are not part of the stop state. | **directly_supported — R3 pp.4–5,13 + R5 pp.4–6 + L2 lines169-216 + L5; 2026-08-08; scope=operation comparison, not a claim of valid e-values on BARAM.** |
| Statistical-transfer boundary | POPPER’s guarantee requires each new e-value to be conditionally valid given prior information and requires test design not to inspect the unused data; it warns that one-hypothesis Type-I control is not multiple-discovery control. | BARAM candidates are adaptively designed after repeated use of the same development folds, so a POPPER-style optional-stopping guarantee cannot be asserted from existing receipts. | Keep deterministic budgets and multiplicity accounting now; make no future anytime-valid claim until a genuinely conditionally valid evidence stream is designed. | **near_match_only — R3 Assumptions 2–3/Theorem4 p.4 and limitations p.15 + `research/nodes/registry.json:protocol`; 2026-08-08; scope=boundary on statistical transfer to repeatedly reused chronological OOF folds.** |

## 3. What the current router already gets right

- Preserve the pure, deterministic `Evidence,state -> RouterDecision` boundary and frozen tie-breaking; it is a suitable place to enforce new guards without changing framework identity. **directly_supported — L1 lines400-432 and L2 lines183-246; 2026-08-08; scope=current deterministic router semantics.**
- Preserve the semantic distinctions “low information” (C8), “measured negative” (C14), “sign anomaly” (C5), and “premise flip” (C9); the repair is to make their evidence and premise dependencies typed and mandatory. **directly_supported — L1 lines212-221,269-290,352-368; 2026-08-08; scope=current useful condition partition.**
- Preserve the one-service C10 rule; the missing condition is that any terminal decision must also pass ledger reconciliation, stage coverage, and budget/evidence accounting. **near_match_only — L2 lines169-216 + R1 Def.7 p.34 + R3 Algorithm1 p.13; 2026-08-08; scope=minimal guard extension, not router replacement.**

## 4. S9–S11 workflow delta

| Project stage | Small operation delta | Non-duplication with M271 | Evidence |
|---|---|---|---|
| S9 문제해결접근 | Add a `HypothesisCard`: claim, exact premise IDs, measurable refuter, control/null, primary metric, minimum effect, and tri-state decision rule (`SUPPORTED / REFUTED / INCONCLUSIVE`) committed before execution. | C2/C5/C14 remain the downstream routes; the card makes their input admissible rather than adding another router. | **near_match_only — R3 pp.2,4–5,16 + L1 C2/C5/C14; 2026-08-08; scope=POPPER contract shape transferred without claiming its e-value guarantee.** |
| S10 모델개선전략 | Replace C17’s `(lane,kind)` count with a deterministic structured novelty signature over stage, mechanism, treatment, control, data, metric, topology, and parent; retain the last-6 throttle only as a fallback. Add `gain_point`, conservative `gain_upper`, cost, and basis evidence IDs to C16/C11. | C16/C17 and C11 stay in place; only their input evidence becomes discriminating and auditable. | **near_match_only — R4 §§3.1–3.2 + R5 §§2–3 + L1/L2; 2026-08-08; scope=archive/acquisition semantics adapted to typed experiment metadata, no embedding dependency.** |
| S11 분석방법적절성 | Make one append-only event stream the source for graph, ledger, coverage, premise state, comparison count, and halt eligibility; add reconciliation/soundness checks before close, revive, promotion, or halt. | Existing LangGraph checkpoints and M271 decisions remain execution mechanisms; the new reducer is an enforcement envelope around them. | **near_match_only — R1 Def.7 + R6 §§4.3–4.4 + R7 provenance model + L3–L6; 2026-08-08; scope=minimal event/provenance enforcement.** |

## 5. Deterministic invariants and test cases

| ID | Invariant | Deterministic test / expected result | Evidence |
|---|---|---|---|
| INV-01 exact cycle accounting | Every discovered/executed cycle ID is in exactly one of `CLOSED`, `PENDING`, or `LEGACY_UNRECONSTRUCTABLE`; a count-only audit never satisfies ID reconciliation. | Load the current `CYCLES` set and explicit ledger IDs: assert the missing set is exactly the receipt’s 88 IDs and block `HALT`, closure, and promotion. After repair, assert set equality, while the 88 legacy IDs remain explicitly non-evidentiary. | **near_match_only — R7 automatic provenance + L4 `unrecorded_cycles` + L5 `unrecorded_cycles=88`; 2026-08-08; scope=local ledger reconciliation.** |
| INV-02 two-phase, exactly-once cycle state | A cycle has one immutable `PREDECLARED` event before execution and at most one `CLOSED` event referencing the preregistration hash; graph/ledger views are derived from these events. | Inject crashes before predeclare, after predeclare, during result write, and after close; replay must yield respectively absent, resumable `PENDING`, resumable `PENDING`, or one `CLOSED` cycle—never a closed graph node without a ledger event. Repeating close with the same ID is idempotent; different payload is rejected. | **near_match_only — R7 engine/provenance and immutable-node sections + L3 optional `close_cycle`; 2026-08-08; scope=exactly-once filesystem event contract.** |
| INV-03 provenance completeness | A closed score-bearing event must carry receipt path+SHA-256, code/config/data identity, policy, in-sample-vs-fold-outside status, row-alignment keys, parent IDs, actor, and timestamps. | Delete each required field in turn and corrupt one hash: validation must fail before routing. Recompute every hash on replay and require the same state digest. | **near_match_only — R7 provenance/node/reproducibility sections + `AGENTS.md` standing receipt rule + L1 Evidence fields; 2026-08-08; scope=BARAM provenance schema.** |
| INV-04 canonical S1–S11 coverage | The canonical stage set is exactly S1…S11; each stage needs an evidence-bearing `RESEARCHED`, an explicit scoped `INSUFFICIENT`, or an approved `NOT_APPLICABLE` record before terminal completion. A model fit is not mandatory for a stage. | Feed `S12-N21_workflow_audit.json`: expected missing dedicated-research set is `{S1,S2,S4,S7,S8,S10,S11}` and `can_halt=False`. Add one valid record per missing stage: expected coverage `11/11`; S11 may close only with a method-appropriateness verdict. | **near_match_only — R1 Def.6/7 + R2 §3.2.1 + L6; 2026-08-08; scope=stage coverage without forcing pointless experiments.** |
| INV-05 graph soundness | Every admitted node is reachable from a start node and has a path to exactly one terminal verdict; no stage transition is dead; terminal state has no unconsumed runnable token. | Add an orphan, a dead S11 transition, and a terminal with a still-runnable child; each must fail. A normal branch ending in `REFUTED` must pass because negative completion is still completion. | **near_match_only — R1 Def.6 p.17 and Def.7 p.34; 2026-08-08; scope=WF-net soundness mapped to an experiment DAG.** |
| INV-06 falsification admissibility | Every mechanistic claim names a logically connected refuter/control and a frozen decision rule; outcomes are tri-state, and `INCONCLUSIVE` cannot set `negative_finding=True`. | Missing null/refuter/control/min-effect fields must be refused. A test whose subclaim is not implied by the parent must be refused. A low-information result must route C8, not C14; a measured negative above the threshold may route C14. | **near_match_only — R3 pp.2,4,16 + L1 C8/C14; 2026-08-08; scope=contract validation without importing domain-specific p-values.** |
| INV-07 typed closure dependencies | Every closure has unique `closure_id`, exact `premise_ids`, supporting evidence IDs, scope, and dependent node IDs; closure state is a reducer output, never an unlinked string. | Flip one premise false: exactly its transitive dependent closures become `INVALIDATED` and one `REVIVE` event per frontier node is emitted. Replay the flip: no duplicate revival. Restore the premise: recompute deterministically; unrelated closures remain byte-identical. | **near_match_only — R6 §§4.3–4.4/4.9 + L1 C9/C14; 2026-08-08; scope=small ATMS-like dependency reducer, not full ATMS adoption.** |
| INV-08 discriminating novelty | Novelty identity is computed from normalized experimental content, not display labels or lane frequency; the archive stores the signature with its evaluated result. | Rename an otherwise identical proposal and move it to another lane: it must still be duplicate. Submit three proposals in one lane/kind with distinct mechanisms/treatments: none is rejected merely because the counter reaches three. Same signature with a changed parent is either a declared replication or rejected. | **near_match_only — R4 §§3.1–3.2 + L2 C17; 2026-08-08; scope=deterministic structured proxy for semantic novelty.** |
| INV-09 magnitude/cost evidence | C16/C11 inputs include `gain_point`, `gain_upper`, cost, remaining gap, and basis evidence IDs; a hard prune uses the conservative upper bound, while ranking states explicitly that it is heuristic unless calibrated. | Missing basis/cost/upper bound is inadmissible. If `gain_upper < floor*gap`, reject; if only the point is below but the upper bound crosses, do not hard-prune. For equal gain evidence, lower cost ranks first. | **near_match_only — R5 §§2–3/Cor.3.5 + L1 C11 + L2 C16; 2026-08-08; scope=conservative transfer where no valid BO posterior presently exists.** |
| INV-10 adaptive comparison accounting | Every score-bearing look increments one monotone `comparison_index` exactly once and stores the predeclared threshold/version; diagnostic-only nodes state that they cannot promote. | Replay N committed score looks and require indices `1..N` with no gaps/duplicates. Attempt promotion using a stale index or threshold chosen after the result: reject. The legacy 88 cannot be silently assigned invented comparison outcomes. | **near_match_only — R3 pp.4,13,15 + L5 legacy gap; 2026-08-08; scope=multiplicity ledger; no anytime-valid claim on reused folds.** |
| INV-11 halt eligibility | `HALT` is allowed only for target reached, hard budget exhausted, or `(frontier empty AND C10 serviced exactly once AND ledger reconciled AND S1–S11 covered)`; stall is computed only from committed fresh events. | Current state (`stall=97`, missing=88) must return `HALT_BLOCKED_LEDGER_GAP`. After ledger reconciliation but current coverage audit, return `HALT_BLOCKED_STAGE_COVERAGE`. With both repaired and no frontier, repeated evaluation returns the identical terminal reason/digest. | **near_match_only — R1 soundness + R2 stage budgets + R3 max-test stop + R5 matched stop + L2/L5/L6; 2026-08-08; scope=deterministic composite stop guard.** |
| INV-12 promotion separation and replay | Router actions (`RESEARCH_DIRECTION`, `CLOSE_AXIS`, `REVIVE`, `REJECT_DIRECTION`) cannot mutate champion state; only a separate `PROMOTION` event with complete preregistration/provenance/comparison evidence can do so. Folding the event log twice yields the same state digest. | Property table over every router action asserts champion unchanged. Remove or reorder a parent event: replay rejects causal inconsistency. Replay the intact log twice: graph, ledger, coverage, closures, comparison count, and champion digests are identical. | **near_match_only — R7 typed DAG/immutable provenance + R6 justification dependency + L1/L2 actions; 2026-08-08; scope=event-sourced promotion/replay guard.** |

## 6. Honest repair of the observed 88-cycle legacy gap

1. Emit one immutable `LEGACY_GAP_v1` manifest containing the **sorted exact 88 IDs**, any extant receipt paths/hashes, and per-ID status `LEGACY_UNRECONSTRUCTABLE`; do not use only `cycles_covered=N`, and do not invent per-cycle score, premise, or stage deltas. **near_match_only — L5 exact missing count/sample + L3 audit design + R7 provenance model; 2026-08-08; scope=honest legacy accounting.**
2. Separate `accounted` from `evidence_complete`: the manifest can make every historical ID accounted for, but none of those 88 may close a stage/axis, reduce deficit mass, increment a reconstructed comparison outcome, or support promotion without a directly matching old receipt. **near_match_only — R7 immutability/traceability + R6 justification semantics + L5; 2026-08-08; scope=preventing fabricated backfill.**
3. Start a `ledger_schema_v2` epoch whose canonical state is reduced only from `PREDECLARED`/`CLOSED` events; keep the old JSON and manifest as immutable ancestors so rollback/audit remains possible. **near_match_only — R7 data/logical provenance + L4 append/reducer; 2026-08-08; scope=minimal forward repair.**

## 7. Smallest delta to the current workflow

- Add one typed `CycleEnvelope` and one event-owning wrapper around existing execution: `commit PREDECLARED -> run -> validate -> decide_v3 -> atomically commit CLOSED`; derive graph, ledger, stage coverage, premise state, comparison count, and halt state from those events. No new orchestration brand or dependency is required. **near_match_only — R7 engine/provenance + L1/L2 pure router boundary; 2026-08-08; scope=small enforcement envelope.**
- Extend, rather than replace, three v3 inputs: C17 reads a structured novelty signature; C16/C11 read gain bound+cost+basis; C9 reads exact flipped premise IDs and a dependency reducer. Preserve C1–C15 actions, C16/C17 names, frozen tie-breaks, and one-shot C10. **near_match_only — R4/R5/R6 + L1/L2; 2026-08-08; scope=small router-input delta.**
- Add two mandatory preflight gates: `reconcile_cycles()==0 unresolved forward gaps` and `coverage_debt()==0` before terminal/promotion; while debt exists, enqueue the lexicographically first least-covered S1–S11 stage as a bounded research node, with an explicitly scoped non-answer allowed as an honest result. **near_match_only — R1 soundness + R2 staged checkpoints + L5/L6; 2026-08-08; scope=repairing exactly the observed ledger and coverage failures.**
- Do not add e-values or claim optional-stopping validity on the current reused folds; initially enforce only a hard comparison budget, monotone comparison ledger, frozen decision thresholds, and explicit exploratory scope. **near_match_only — R3 Assumption2/Theorem4/limitations + local registry protocol; 2026-08-08; scope=statistically conservative stop repair.**

