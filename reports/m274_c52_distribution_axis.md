# M274 (정정본) — C5.2 분포·분위수 예측 축 개방

- 최초 작성 2026-08-06, **정정 2026-08-06 (M275 결과 반영)**
- 적합 0회 / 락박스 미접근 / 제출물 없음

## 0. 철회된 주장 (자기정정)

본 리포트 초판은 두 가지를 틀리게 주장했다. 둘 다 **원본을 읽기 전에 쓴 것**이다.

| 철회 주장 | 실제 | 확인 경로 |
|---|---|---|
| "게이트가 pinball 로 후보를 선택한다" | `_distribution_gate` 는 **6개 조건의 논리곱 진입게이트**다. 순위선택이 아니다. | `src/baram/v2_workflows.py::_distribution_gate` |
| "공식지표 3.7배 나은 D2 를 기각했다" | **N401 재검증(2026-08-06)**: 매니페스트의 `q50_official.total` 은 후보 공통 키(13,233행) 기준으로 **일관되게** 계산돼 있다. D1 을 그 기준으로 재계산하면 `0.5687951243` 으로 매니페스트 값 `0.5687951242709349` 와 **소수점 10 자리 일치**한다. 어긋난 것은 매니페스트가 아니라 **M275 의 재계산 기준**(Q2 포함 19,785 행)이었다. 초판의 "3.7 배" 주장은 여전히 철회하되, 그 사유는 '매니페스트 정렬 오류' 가 아니라 **내가 다른 행집합으로 비교한 것**이다. | N401 영수증 |

D2 의 실제 기각 사유는 기록되어 있었다 — `reasons=['pooled_coverage']`.

## 1. 후보 8종 (dev-2023, 동일 적격행 11,486 기준 재계산)

| 후보 | q10-q90 | 밴드ECE | 밴드bias | q50 Total | 채택 |
|---|---:|---:|---:|---:|:--:|
| D0_EMPIRICAL_RESIDUAL | 0.7595 | 0.2370 | +0.2370 | 0.565619 | control |
| **D1_LGBM_SHARED_BASE** | 0.7199 | **0.0765** | +0.0710 | **0.575443** | **✅** |
| D2_LGBM_SHARED_LEAF63 | 0.6952 | 0.0889 | +0.0889 | 0.575188 | ❌ `pooled_coverage` |
| D3_LGBM_GROUP_BASE | 0.6924 | 0.1019 | +0.0994 | 0.571051 | ❌ |
| D4_LGBM_GROUP_CHILD80 | 0.7067 | 0.0971 | +0.0944 | 0.568721 | ❌ |
| D5_QRF_LEAF40 | 0.8617 | 0.0843 | +0.0165 | 0.567450 | ❌ |
| D6_QRF_LEAF80 | 0.8666 | 0.0869 | +0.0174 | 0.564096 | ❌ |
| D7_CHRONO_CONFORMAL | 0.8906 | 0.0613 | +0.0612 | 0.568795 | ❌ |

**게이트는 올바른 후보를 채택했다.** 밴드 캘리브레이션·공식지표 어느 쪽으로 봐도 D1 이 D2 보다 낫다.
D2 재판정은 정당화되지 않는다.

## 2. 확립된 것 — 전 후보의 밴드적중 과대예측

예측 밴드적중확률 `0.276~0.504` 대 실제 적중률 `0.259~0.279`. **bias 가 전부 양수**다.
채택된 D1 도 `+0.0710` 과대예측(0.3485 예측 / 0.2775 실제).

## 3. 미해결 — `[0.72, 0.88]` 의 근거

이 값은 저장소 전체에서 `.planning/2026-08-01-leaderboard-top-4-loop/IP_v2.md:91` **한 곳에만** 나오고
유도가 없다. 같은 표의 Total 문턱은 관측 시드분산 `0.003014` 에서 유도하고 과거 두 사례로 검증까지 했다.
채택된 D1 의 적격행 기준 피복은 `0.7199` 로 하한 `0.72` 에 소수점 4자리로 걸린다.
게이트 전제(`명목에 가까울수록 좋다`)는 8후보 상관 `rho=+0.643 (p=0.086)` 으로 **부호가 반대**이며
지지되지 않는다. 다만 n=8 이므로 `near_match_only` 로 태그한다.

## 4. 살아남은 신호

밴드적중 과대예측 `+0.0710` 이 `C9.2 캘리브레이션` 의 타깃이다.
M272 가 측정한 결정층 최적온도 `T≈2.0`(>1 = 분포를 무르게)과 방향이 일치한다.
→ M276 에서 두 현상이 **동일 현상인지** 검정한다.
