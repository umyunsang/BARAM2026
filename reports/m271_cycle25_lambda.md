# M271 P4 사이클 25 — 봉투 안 위치는 예보시점에 예측 가능한가

- 판정일: 2026-08-04 (UTC)
- 노드: `C1N25_ENVELOPE_LAMBDA` / 레인 L3 / 부모 `C1N19_ENVELOPE_ORACLE`
- 기존 승격후보 `M271_MEDIAN4` Total **0.636597**
- 유효행 11,486. 추정은 유효행에서만 (사이클 23 의 교훈)
- 게이트: `M270_MONTHLY_GATE_v1_frozen_2026-08-04` (읽기만 함)

## 0. 방법 리서치 (실행 전)

- **Wolpert (1992) — Stacked Generalization** (`directly_supported`)
  - 교차검증 예측 위에 상위 학습기를 얹어 결합한다
  - 사용: fold-외 추정 설계의 근거
- **Breiman (1996), Machine Learning — Stacked Regressions** (`directly_supported`)
  - 비음·합1 가중치는 추정량을 **보간적**으로 만들어 min<=pred<=max 를 보장하고, 그 제약이 최고 단일 모델 선택보다 나은 정확도의 이유다
  - 사용: 결합을 가중치 4 개가 아니라 **봉투 안 위치 lambda 1 개**로 재좌표화. 사이클 19 의 봉투 오라클이 곧 이 제약 스태킹의 천장임을 확정
- **forecast combination puzzle (Clemen 1989 이후 반복 확인)** (`contradicts_premise`)
  - 추정 가중치는 흔히 단순평균에 진다
  - 사용: 자유 회귀를 기각하고 **층별 상수 lambda** 로 자유도를 최소화

## 1. lambda* 분포 (H1)

`lambda* = clip((actual - min) / (max - min), 0, 1)` — 봉투 안 최적 위치.

| 위치 | 질량 |
|---|---:|
| lambda* <= 0.01 (최저 멤버 이하) | 0.4516 |
| 내부 0.01~0.99 | 0.2218 |
| lambda* >= 0.99 (최고 멤버 이상) | 0.3266 |
| **양극단 합** | **0.7782** |

참고로 현 `median` 결합자의 실효 위치는 평균 0.6094, 중앙값 0.6696 이다.

## 2. 문맥이 위치를 가르는가 (H2 · H3)

굵은 층 36 개, lambda 중앙값 층간 범위 **0.7828**.

| 층 (그룹\|예측대역\|스프레드) | 행 | lambda 중앙값 | lambda 평균 |
|---|---:|---:|---:|
| `3|(0.7, 1.1]|mid` | 432 | **0.0000** | 0.3820 |
| `3|(0.45, 0.7]|lo` | 186 | **0.0000** | 0.4350 |
| `1|(0.25, 0.45]|lo` | 249 | **0.0000** | 0.4320 |
| `2|(0.7, 1.1]|lo` | 816 | **0.0000** | 0.3649 |
| `2|(0.45, 0.7]|mid` | 303 | **0.0000** | 0.3738 |
| `2|(0.45, 0.7]|lo` | 172 | **0.0000** | 0.4223 |
| `3|(0.7, 1.1]|lo` | 251 | **0.0000** | 0.4043 |
| `2|(0.7, 1.1]|mid` | 405 | **0.0050** | 0.3724 |
| `2|(0.1, 0.25]|lo` | 402 | **0.4719** | 0.4969 |
| `1|(0.7, 1.1]|mid` | 476 | **0.4995** | 0.4954 |
| `2|(0.1, 0.25]|mid` | 281 | **0.5149** | 0.4891 |
| `3|(0.1, 0.25]|hi` | 128 | **0.5405** | 0.5054 |
| `2|(0.1, 0.25]|hi` | 117 | **0.5549** | 0.5144 |
| `1|(0.7, 1.1]|lo` | 377 | **0.5874** | 0.5119 |
| `1|(0.1, 0.25]|mid` | 364 | **0.6229** | 0.5363 |
| `1|(0.45, 0.7]|lo` | 220 | **0.7828** | 0.5143 |

fold 간 층 순위 안정성:

| fold 쌍 | 공통 층 | Spearman |
|---|---:|---:|
| Q2~Q3 | 36 | **-0.2425** |
| Q2~Q4 | 36 | **+0.1740** |
| Q3~Q4 | 36 | **-0.1582** |

평균 **-0.0756**

## 3. fold-외 lambda 대체 (H4)

| 추정량 | Total | 1-NMAE | FICR | 기존대비 | G1G2G3G4 | 양수월 | q05 | 자격 |
|---|---:|---:|---:|---:|:---:|---:|---:|:---:|
| `M271_MEDIAN4` (기존) | 0.636597 | 0.859984 | 0.413210 | — | — | — | — | — |
| `median_lambda` | 0.614104 | 0.863880 | 0.364329 | -0.022493 | `----` | 1/9 | -0.030330 | 미달 |
| `metric_lambda` | 0.633256 | 0.857285 | 0.409227 | -0.003341 | `----` | 2/9 | -0.008797 | 미달 |

## 4. 사전확약 대조

- H1 `lambda* 양극단 질량 >= 0.60` -> **True** (실측 0.7782)
- H2 `층별 lambda 중앙값 층간 범위 >= 0.15` -> **True** (실측 0.7828)
- H3 `fold 쌍 Spearman 평균 >= 0.50` -> **False** (실측 -0.0756)
- H4 `적어도 하나가 Total 개선 + 게이트 통과` -> **False**

판정: **EXTRACTION_AXIS_CLOSED_LAMBDA_UNPREDICTABLE**

승격 Total **0.636597**, 목표 0.66 까지 **+0.023403**.

