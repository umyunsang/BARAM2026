# M271 P4 사이클 15 — 시간순 안전 walk-forward 앙상블 가중

- 판정일: 2026-08-04 (UTC)
- 노드: `C1N15_WALKFORWARD_ENSEMBLE` / 레인 L4
- 게이트: `M270_MONTHLY_GATE_v1_frozen_2026-08-04` (읽기만 함)

## 1. 가중 규칙

각 fold 의 가중은 **그 fold 앞** 의 fold 들로만 만든다. 선행이 없으면 등가중.
가중은 선행 fold MSE 의 역수에 비례하며 정규화한다. 온도나 상위 K 컷 같은 자유도를
두지 않는다 — 전부 사전 선언이다.

| fold | 가중 출처 | 상위 3 모델 (가중) |
|---|---|---|
| `dev-2023-Q2` | 등가중 (선행 없음) | `M102_TOP100` 0.083, `M113_LGBM_DART` 0.083, `M115_XGBOOST` 0.083 |
| `dev-2023-Q3` | dev-2023-Q2 | `M244_RARE_EVENT_CORRECTED_ANALOG_Q234` 0.125, `M64B_ALLWEATHER_SITEWIND_CLASS` 0.085, `M102_TOP100` 0.084 |
| `dev-2023-Q4` | dev-2023-Q2, dev-2023-Q3 | `M244_RARE_EVENT_CORRECTED_ANALOG_Q234` 0.116, `M115_XGBOOST` 0.085, `M64B_ALLWEATHER_SITEWIND_CLASS` 0.085 |

## 2. 판정

| 후보 | Total | 델타 | 1-NMAE | FICR | G1 G2 G3 G4 | 양수월 | 개선/악화 | 통과 |
|---|---:|---:|---:|---:|:---:|---:|---:|:---:|
| `WF_RAW` | 0.631422 | +0.002818 | 0.860399 | 0.402446 | `-OOO` | 6/9 | 59/49 | 기각 |
| `WF_SHRINK_a0.5` | 0.630984 | +0.002380 | 0.858474 | 0.403495 | `-OOO` | 6/9 | 64/44 | 기각 |
| `E1_ALL_EQUAL_reference` | 0.631934 | +0.003330 | 0.860016 | 0.403853 | `-OOO` | 6/9 | 57/51 | 기각 |

## 3. 사전확약 대조

- H1 `walk-forward 가중 앙상블이 동결 게이트를 통과한다` -> **False** (없음)
- H2 `walk-forward 가중이 등가중(E1)보다 pooled Total 이 높다` -> **False** (E1 기준 0.631934)

판정: **WALKFORWARD_REJECTED**

## 4. 한계

Q2 는 선행 fold 가 없어 등가중을 쓴다. 즉 전체 행의 1/3 은 가중의 이점을 못 받는다.
실제 운영에서는 2022 이력이 있으므로 이 제약이 완화되지만, 여기서는 probe 산출물이
2023 Q2~Q4 만 있어 그렇게 하지 못한다.

그리고 이것은 **로컬** 판정이다. 온라인 이전은 별개이며 방법군별 오프셋이 3.2 배까지
벌어진 전례가 있다.

두 변형 모두 사전 선언이다. 어느 쪽이 좋은지 보고 고르는 것은 same-fold 선택이므로
하지 않는다.
