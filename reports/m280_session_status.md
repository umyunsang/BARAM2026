# M280 — 세션 통합 상태 및 목표 격차 감사 (2026-08-06)

- **파생 전용 요약. 신규 적합 0회.** 락박스 미접근 / 제출 없음 / 외부행위 없음.

## 1. 목표 대비 위치

| | 값 |
|---|---:|
| 목표 | 온라인 Total **0.66** (상위권 진입 0.67) |
| 현재 최고 온라인 실측 | **0.6365274** (M261, 2026-08-04 사용자 업로드) |
| 현재 최고 로컬 조합 | **0.639389** (C1N99, med 결합) |
| 최고 조합의 예상 온라인 | **0.6459 ~ 0.6605** (중심 추정 0.6489) |
| 목표까지 잔여 | **+0.0111** (중심 추정 기준) |

예상 구간이 넓은 이유는 로컬→온라인 오프셋이 **방법 계급별로 다르기** 때문이다
(분류기 `+0.006554` / 아날로그 `+0.021119`, 3.2배 차). 최고 조합은 5 멤버 중 아날로그 1 개이고
결합자가 median 이므로 **하단에 가까울 것으로 본다.**

## 2. 이번 세션이 닫은 축 (전부 사전확약 후 판정)

| 노드 | 판정 | 근거 |
|---|---|---|
| M275 | 분포 게이트는 **정당** | 동일 적격행 재계산 시 D1 > D2. 초판의 "D2 가 3.7배 낫다" 는 철회 |
| M276 | `DECISION_TEMPERATURE_IS_CALIBRATION` | T_cal 1.731 vs T* 2.00, 핵심가설 H3 통과 |
| M277 | `CONDITIONAL_CALIBRATION_EXHAUSTED` | 온도 1→3→9 개로 세분화할수록 단조 악화 |
| M278 | `BAND_GAIN_IS_CALIBRATION_ARTIFACT` | 팔별 최적화 시 +0.009772 → +0.000374 (96.2% 축소) |
| M279 | `EXTENSION_DOES_NOT_TRANSFER` | 배포 계보에서 격자확장 개선 **정확히 0** |

**획득 점수 0.** 대신 `C1N41` 의 `+0.021490`(잔여 격차와 맞먹던 최우선 후보)이 실체 없음을 확인했고,
결정층 온도의 정체를 규명했다. 남은 기간을 잘못된 방향에 쓰는 것을 막은 것이 실질 산출이다.

## 3. 구조 산출물

- `artifacts/registry/capability_ontology_v1.json` (v1.1) — 10 카테고리 / **서브능력 55** / 선행조건 엣지 56, 비순환
- `reports/m273_capability_coverage.md` — 기존 136 노드 중 116 매핑, 커버리지 공백 노출
- `reports/ADVERSARIAL_REVIEW_2026-08-06.md`
- `inputs/` — 불변 입력 통합(해시 2건 동결값 일치), `AGENTS.md` 갱신

기존 그래프는 `category` 필드가 없어 커버리지가 감사 불가였다. 온톨로지가 그것을 드러냈고,
`C5.2`(분포·분위수)가 **구현되어 있으나 그래프에 없는 선행조건**임을 찾아 `C5.3`/`C9.2` 차단을 풀었다.

## 4. 루프 상태

라우터 `M271_ROUTER_v3` 판정: **`TERMINATION_GUARD → HALT`** (정체 카운터 97 / 한계 3).
이번 세션이 5 개 축을 추가로 닫았으므로 정체는 완화되지 않는다.

## 5. 목표 도달에 필요한 것 (증거 기반)

닫힌 것: 후처리 · 결정정책 · 캘리브레이션(전역/조건부) · 밴드타깃 · 전단 융합(C1N77) · 외부 NWP · 시퀀스 표현.

프로젝트 자체 분석의 결론이 유지된다 — **표현이 바뀌어야 한다.**
정량 요구치: 예측오차 **11.0% 감소** (m270_revised_verdicts, k=0.890).
다중소스 평균으로 얻을 수 있는 최대치는 **4.6%** (LDAPS-GFS 오차상관 0.78).

## 6. 즉시 가능한 유일한 실질 개선

최고 로컬 조합(`C1N99`, 0.639389)은 **아직 제출된 적이 없다.** 최고 온라인은 M261(로컬 0.629973)이다.
로컬 `+0.009416` 우위가 전이되면 온라인 `0.6365 → 약 0.649` (**+0.012**).

목표 0.66 에는 미달하나 현재 최고 대비 명확한 개선이며, 동시에 **혼합 계보의 오프셋을 측정**하는
정보가치를 갖는다. 업로드는 사용자만 수행한다(에이전트 업로드 금지 규칙 유지).

## 7. 목표 미달 선언

`0.66` / `0.67` 은 **달성되지 않았다.** 현재 증거로 도달 가능한 상한은 약 `0.66` 의 낙관 시나리오
(아날로그 오프셋 전량 적용)이며 중심 추정은 `0.649` 다. 목표는 열린 상태로 유지한다.
