# SK@v4 (withdrawn) → SK@v5 Field-Level Diff

## Identity

- Proposed artifact: `SK@v5`
- Stable locator: `.planning/2026-08-01-leaderboard-top-4-loop/SK_v5.md`
- SHA-256: `c2c813475ab5f0a741e6adbceab196d5b38447f2aeb02f9716a8f5be8973c820`
- State: `SKELETON_APPROVAL_PENDING(SK@v5)`
- Exact approval phrase: `SK@v5 승인`
- Prior `SK@v4`: unapproved and withdrawn; it is history, not an authority base.

## Material changes

| Field | Withdrawn SK@v4 / current S17 interpretation | Proposed SK@v5 | Reason |
|---|---|---|---|
| Discovery meaning | Search for an exact-scope directly supported operation before emitting a node | Generate falsifiable local migration hypotheses from direct source facts and/or reproducible local diagnostics | The user explicitly rejects “find an existing path” as the task |
| Evidence vs transfer | `near_match_only` cannot produce any child; transfer requires exact published BARAM-like treatment | Near evidence never proves transfer, but may navigate to a separately labelled provisional hypothesis with a complete migration contract | Preserve evidence integrity while enabling scientific inference |
| Analyst choices | Source/provider must collapse choices to one zero-choice formula | Motivated choices are allowed if frozen, counted, bounded and falsifiable before local evaluation | Local migration necessarily specifies competition-specific choices |
| Depth | Stage index S1→S11 is reported as depth | Separate `stage_depth` from recursive `node_depth` inside each stage | The current graph cannot descend within preprocessing/features/models |
| Node schema | Spec ID/kind/subcapability/summary plus execution fields | Adds parents, deficit, research claims, migration gaps, DOF, mechanism, falsifier, residual child and re-entry/closure rules | Needed for audit-ready result-conditioned descendants |
| Stage completion | Exact node adjudicated or all findings near/insufficient | Foundation + research + migration matrix + feasible experiment + adjudication + residual child + appropriateness decision | Prevent shallow binary coverage |
| Re-entry | Primarily exact contradiction | Any diagnosed downstream failure can re-enter the causal upstream stage; contradiction remains mandatory | The user's loop is result-driven |
| Terminal semantics | All stages/empty frontier may yield `HALT_ALLOWED_EXHAUSTED` below target | Below-target exhaustion is a resumable research pause; only reproduced `Total >= 0.660000` is success | Aligns with the explicit exit condition |
| Research topology | Six broad lanes, then smallest single exact candidate | Sequential foundation wave, then module-specific SOTA/benchmark research and a bounded sibling portfolio | Supports deep pipeline expansion rather than one-candidate intake |
| Local experiment role | Literature gate determines whether execution is allowed | Local strict experiment is the transfer adjudicator; source effect sizes are not promotion evidence | Separates external validity from local evidence |
| External inputs | Eligibility lane retained | Same current `AGENTS.md` availability/licence/symmetry boundary; no body retrieval under SK approval | Unchanged hard constraint |

## Unchanged binding fields

- Strict 72-cell issuance chronology and strictly past-only fit/selection/label availability.
- Append-only EventStore, exact row alignment keys and comparison accounting; next score-bearing index is 5.
- Immutable competition/baseline hashes and project-local Python 3.12.
- Root-only repository writes/fits; no more than six model workers.
- No further 2024 score, slice, calibration or selection.
- No Dacon/account/team mutation, remote API inference, reanalysis or test-period observations.
- Exact-scope PASS labels; local evidence never proves leaderboard position.
- Strict local official `Total >= 0.660000` reproduced success threshold.

## Approval scope

`SK@v5 승인` authorizes only the bounded read-only research manifest and bounded local audit in the skeleton. It does not authorize code changes, model fits, dependencies, external data bodies, Dacon, remote actions or 2024 evaluation. `DS@v5` and `IP@v3` remain separate approval gates.
