# M279 — M272 격자확장이 배포 계보에 전이되는가: 반증

- 판정일 2026-08-06 · 노드 `M279_DEPLOYED_GRID_TRANSFER` · **적합 0회** / 락박스 미접근 / 제출물 없음
- 표면: `artifacts/backtests/m269-probe/M269_PROBE-*-probability.npz` (M102_TOP100 배포 계보)

## 1. 사전확약 대조

| | 결과 |
|---|---|
| V1 배포 `T0.5_G1.5` 가 0.628605 재현 (허용 5e-4) | **True** (0.628337, 오차 2.7e-4) |
| H1 확장격자 최적 > 기존격자 최적 | **False** (차 +0.000000) |
| H2 fold-외 선택이 배포 초과 **[핵심]** | **False** (0.628080 vs 0.628337, −0.000257) |
| H3 3/3 폴드 개선 | **False** (−0.000976 / −0.000014 / +0.000112) |
| H4 배포최적 T ≠ 재구성최적 2.0 | **True** (배포최적 T=0.5) |

판정: **`EXTENSION_DOES_NOT_TRANSFER`**

## 2. 결과

| | 정책 | pooled Total |
|---|---|---:|
| 배포 | T0.5_G1.5 | 0.628337 |
| 기존격자(T<=1.2, G<=2.0) 최적 | T0.5_G0.2 | 0.629150 |
| 확장격자(T<=3.0, G<=6.0) 최적 | T0.5_G0.2 | 0.629150 |

확장 구간에서 **개선이 정확히 0** 이다. 배포 계보의 최적은 기존 격자 **내부**에 있었다.
M272 의 `+0.010483` 은 재구성 표면 고유의 결함 교정이며 배포 개선 경로가 아니다.

## 3. M276 확증

H4 통과. 배포 표면 최적 `T=0.5`(뾰족하게) vs 재구성 표면 최적 `T=2.0`(무르게) — **정반대**다.
M276 의 `DECISION_TEMPERATURE_IS_CALIBRATION` 이 예측한 "최적 온도는 모델별" 이 그대로 관측됐다.
배포 모델은 과소확신, 재구성 모델은 과대확신이다.

## 4. 배포 정책 축 소진

`T0.5_G0.2` 는 인샘플 `+0.000813` 이지만 fold-외 선택 시 `−0.000257` 로 뒤집힌다.
그 차이는 선택 노이즈이며 **배포 정책을 교체할 근거가 없다.**
결정정책 축은 배포 계보에서 닫힌다.
