# M271 P4 — 발굴 가동 1단계: N0 증거 라우팅 실측

- 판정일: 2026-08-04 (UTC)
- 결손 원장: 셀 108, 미설명 질량 0.371395
- 로컬 Total 0.628605 / 목표 0.66 / 격차 0.031395
- 발굴 그래프: 노드 116, 프론티어 116

N0 의 8 개 노드가 낸 **실제** 증거를 receipt 에서 읽어 동결 라우터 표에 그대로
통과시켰다. 증거를 손으로 고쳐 넣지 않았다.

## 라우팅 결과

| 노드 | 레인 | 결손셀 | 정보량 | 부호 | 발화 | 동작 | 검토된 조건 |
|---|---|---|---:|---:|---|---|---|
| `A1_labels` | L1 | `-` | 1.000 | 0 | **C13** | `RESEARCH_DIRECTION` | C13 |
| `A2_columns` | L2 | `-` | 0.414 | 0 | **C14** | `CLOSE_AXIS` | C14 |
| `A3_spatial` | L2 | `-` | 1.000 | 0 | **C13** | `RESEARCH_DIRECTION` | C13 |
| `A5_scada` | L3 | `-` | 0.600 | 0 | **NONE** | `HALT` | - |
| `A6_timing` | L1 | `-` | 0.300 | +1 | **NONE** | `HALT` | - |
| `A4_error` | L2 | `group_id=3|month=2023-12|y_band=(0.45, 0.7]` | 0.622 | +1 | **C12** | `REFINE_AXIS` | C1, C12 |
| `A7_deficit_init` | L8 | `-` | 1.000 | +1 | **C15** | `SEND_FANOUT` | C15 |

## 노드별 근거

### `A1_labels` -> C13

lag-1 자기상관 0.9623 -> 유효표본 급감. 행을 이 축으로 쪼갤 수 없으므로 분할 축이 아니라 구조적 사실이다.

- 표적: `structural_consequence` / 레인 `L1` / 범위: 구조적 사실 'effective_sample_size' 이 이 레인의 접근에 미치는 결과

### `A2_columns` -> C14

진짜 미사용 컬럼 평균 MI 가 선언 컬럼의 0.414 배. 정보를 냈고 그 정보가 부정이므로 확인된 부정(C14)이지 정보 미달(C8)이 아니다.

- 표적: `None` / 레인 `L2` / 범위: None

### `A3_spatial` -> C13

17 기 전 터빈의 최근접 GFS 격자가 1 개. 격자 해상도와 단지 규모의 불일치는 기존 결손 축으로 설명되지 않는다.

- 표적: `structural_consequence` / 레인 `L2` / 범위: 구조적 사실 'grid_resolution_mismatch' 이 이 레인의 접근에 미치는 결과

### `A5_scada` -> NONE

나셀-NWP 잔차 표준편차 최대 2.365 m/s. 직전 세션 sigma_v 를 독립 재현했다.

- 표적 없음

### `A6_timing` -> NONE

사전확약 유지=True. 여유는 {1.0h, 25.0h} 두 값뿐이고 위반 0 건.

- 표적 없음

### `A4_error` -> C12

사전확약 HELD. 편상관 최대 0.0311. 240도 섹터가 행의 43%·최고풍속·최악오차를 동시에 차지하므로 결손 축 후보다.

- 표적: `None` / 레인 `None` / 범위: None

### `A7_deficit_init` -> C15

가법 분해 잔차 5.551e-17 (허용 1e-09). 판정 ADDITIVE_EXACT.

- 표적: `explain` / 레인 `L2` / 범위: 결손 셀 group_id=3|month=2023-12|y_band=(0.45, 0.7] 의 물리·통계 기전
- 표적: `explain` / 레인 `L2` / 범위: 결손 셀 group_id=2|month=2023-11|y_band=(0.45, 0.7] 의 물리·통계 기전
- 표적: `explain` / 레인 `L2` / 범위: 결손 셀 group_id=3|month=2023-07|y_band=(0.7, 1.1] 의 물리·통계 기전
- 표적: `explain` / 레인 `L2` / 범위: 결손 셀 group_id=3|month=2023-11|y_band=(0.45, 0.7] 의 물리·통계 기전
- 표적: `explain` / 레인 `L2` / 범위: 결손 셀 group_id=3|month=2023-10|y_band=(0.25, 0.45] 의 물리·통계 기전

## 읽는 법

발화하지 않은 것도 판정이다. 조건표가 억지로 방향을 붙이지 않는다는 뜻이다.

결손 셀에는 아직 레인이 없다. 기전이 미상이므로 레인별 질량을 0 으로 두었고, 따라서
C6(레인 기아)가 발화하지 않는 것이 옳다. 기전이 밝혀지면서 질량이 레인에 귀속되고
그때부터 C6 가 의미를 갖는다.
